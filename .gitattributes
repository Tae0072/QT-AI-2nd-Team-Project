# 쉘 스크립트는 LF 강제 — Windows 체크아웃에서도 리눅스/Docker 빌드가 깨지지 않도록.
gradlew text eol=lf
*.sh text eol=lf

# Windows 배치 파일은 CRLF 유지
gradlew.bat text eol=crlf
*.bat text eol=crlf

# 음원(mp3)은 바이너리 — EOL 정규화 금지(번들 시드 음원 포함)
*.mp3 binary
