package com.qtai.domain.qt.client.ai;

/**
 * ai.GenerateAiResponseUseCase 임시 구현체 (Mock).
 *
 * qt 작성 시 AI 피드백을 옵션으로 함께 받을 때 사용 (현재는 더미). 실제 AiService가
 * @Primary로 붙으면 자동 비활성화.
 */
// TODO: @Component
// TODO: implements GenerateAiResponseUseCase
public class GenerateAiResponseUseCaseMock {

    // TODO: generate(memberId, request) → 고정 더미 AiResponse 반환
}
