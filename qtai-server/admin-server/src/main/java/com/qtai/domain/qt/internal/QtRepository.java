package com.qtai.domain.qt.internal;

/**
 * QT 영속성 포트. Spring Data JPA로 구현.
 */
public interface QtRepository {

    // TODO: extends JpaRepository<Qt, Long>
    // TODO: Page<Qt> findByMemberIdOrderByCreatedAtDesc(Long memberId, Pageable pageable);
}
