package com.qtai.domain.note.api;

/**
 * 노트 수정 UseCase 포트.
 *
 * 작성자 본인만 수정 가능.
 */
public interface UpdateNoteUseCase {

    // TODO: NoteResponse updateNote(Long memberId, Long noteId, NoteCreateRequest request);
    //       NoteCreateRequest 재사용 또는 별도 UpdateRequest 도입 판단
}
