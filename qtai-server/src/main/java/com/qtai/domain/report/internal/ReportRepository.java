package com.qtai.domain.report.internal;

/**
 * 신고 영속성 포트. Spring Data JPA로 구현.
 */
public interface ReportRepository {

    // TODO: extends JpaRepository<Report, Long>
    // TODO: boolean existsByReporterIdAndShareSnapshotId(Long reporterId, Long shareSnapshotId);
    // TODO: Page<Report> findByReporterIdOrderByCreatedAtDesc(Long reporterId, Pageable pageable);
}
