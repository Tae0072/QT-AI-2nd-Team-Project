# 기능 명세서 — QT-AI v3.4 기준

> **문서 버전:** v0.7
> **작성일:** 2026-05-18 / **최종 갱신:** 2026-05-19
> **기준 문서:** `07_요구사항_정의서.md` v3.4
> **문서 역할:** F-ID별 상세 동작, 진입점, 예외, 완료 기준 관리
> **연관 문서:** `02_ERD_문서.md` v2.2, `03_아키텍처_정의서.md` v1.2, `04_API_명세서.md` v1.6, `05_시퀀스_다이어그램.md`, `06_화면_기능_정의서.md` v1.4, `07_요구사항_정의서.md` v3.4, `23_도메인_용어사전.md`
> **v0.4~v0.7 보강:** 관리자 UI = 별도 웹(v0.4), `@`멘션 본문 자동 삽입(v0.5), 닉네임 7일 변경 잠금(v0.6), 한글 성경 클라이언트 로컬 저장(v0.7). 자세한 내용은 §변경 이력 참조.

---

## 변경 이력

| 버전 | 날짜 | 작성자 | 주요 변경 |
| --- | --- | --- | --- |
| v0.1 | 2026-05-15 | Codex | `07` F-ID 기준으로 기능 명세 초안 작성 |
| v0.2 | 2026-05-15 | Codex | `02/03/04/05/06` v0.2 기준으로 캐시 상태, 인증 API, 닉네임 나눔 API, 관리자 API, 내부 Interface 정합화 |
| v0.3 | 2026-05-18 | Codex | 02~08 최신본 기준으로 Kakao 인증, 닉네임 나눔, notes/praise-songs API, domain.* 패키지, F-14~F-17을 정합화 |
| v0.4 | 2026-05-19 | T (강태오) | `03_아키텍처_정의서.md` v1.2와 정합화 — §2 기능 공통 규칙에 "관리자 UI 플랫폼" 행 추가(별도 웹 프런트엔드 명시), §3 F-06 행에 관리자 웹 UI 분리·사용자 앱 관리자 화면 금지 명시, §4 도메인별 구현 표에 각 관리자 API는 도메인별 `web/Admin{Domain}Controller`에서 노출됨을 추가, §4.1 호출 클라이언트별 진입점 표 신규(사용자 앱/관리자 웹/시스템 배치 호출 경로 비교). 2026-05-19 강사님 직강 결정 반영. 코드 변경 없음. |
| v0.5 | 2026-05-19 | T (강태오) | `07_요구사항_정의서.md` v3.2 §6.4.1 신규 절(`@`멘션 본문 자동 삽입) 반영 — §2 기능 공통 규칙에 "`@`멘션 본문 자동 삽입" 행 추가, §3 F-03 행에 `@`멘션 인용 흐름·`GET /api/v1/bible/verses` 범위 조회·`note_verses` 메타데이터 기록 명시, §3 F-16 행에 자유 노트 모든 카테고리에서 `@`멘션 사용 가능 명시. 트리거 기호 `@`로 확정(2026-05-19, 회의록 05-18 §12 후속). 코드 변경 없음. |
| v0.6 | 2026-05-19 | T (강태오) | `07_요구사항_정의서.md` v3.3 §F-04/F-10 닉네임 7일 변경 잠금 정책 반영 — §2 기능 공통 규칙에 "닉네임 7일 변경 잠금" 행 추가, §3 F-04 행에 카카오 닉네임 기본값·닉네임 설정 화면(A-03)·잠금 정책·`409 NICKNAME_CHANGE_LOCKED` 응답 명시, §3 F-10 행에 닉네임 정책(가입 지정·중복 불가·7일 잠금) 및 변경 후에도 공유글 스냅샷 자동 변경하지 않음을 명시. 출처: 2026-05-18 바이블서버 회의록 §5. 코드 변경 없음. |
| v0.7 | 2026-05-19 | T (강태오) | `07_요구사항_정의서.md` v3.4 §F-01 한글 성경 클라이언트 로컬 저장 정책 반영 — §2 기능 공통 규칙에 "성경 데이터 저장 위치" 행 추가(한글 로컬 SQLite + 서버 마스터, 영어 서버 마스터 + 온라인 조회), §3 F-01 행에 한글/영어 저장 위치 분리·오프라인 한글 표시·`GET /api/v1/bible/bundle?language=ko` 첫 적재 호출 명시. 노트 로컬 DB 우선(회의록 §2)은 v3.4 §18.1 검토 보류 항목으로 명문화만 됨, 본 문서에는 정식 반영하지 않음. 출처: 2026-05-18 바이블서버 회의록 §1·§3·§4. 코드 변경 없음. |

---

## 1. 문서 목적과 경계

이 문서는 `07_요구사항_정의서.md`의 F-ID를 구현 가능한 기능 단위로 풀어쓴다. 요구사항 포함 여부를 바꾸지 않는다.

| 구분 | 이 문서에서 관리 | 다른 문서에서 관리 |
| --- | --- | --- |
| 상세 동작 | 포함 | 요구사항 원본은 `07` |
| API 진입점 | 요약만 포함 | 상세 요청/응답은 `04` |
| 데이터 참조 | 요약만 포함 | 테이블 기준은 `02` |
| 내부 UseCase | 요약만 포함 | 도메인 경계와 UseCase 기준은 `03` |
| 화면 연결 | 요약만 포함 | 화면별 상태는 `06` |

---

## 2. 기능 공통 규칙

| 항목 | 기준 |
| --- | --- |
| 외부 API | `/api/v1/**` prefix를 사용한다. |
| 내부 호출 | Java Interface로 처리하고, 다른 도메인의 Entity/Service/Repository를 직접 import하지 않는다. |
| AI 호출 | 해설·시뮬레이터 생성은 04:00 KST 배치 또는 관리자 트리거로 수행한다. 사실 기반 Q&A는 단발 요청 후 형식·정책 검증을 통과한 응답만 표시한다. |
| ANONYMOUS | 카카오 로그인 시작/콜백은 ANONYMOUS로 접근한다. |
| 로그인 필요 | 묵상 저장, 해설 본문 열람, 찬양 저장, 묵상 달력 조회는 로그인 필요다. |
| 시뮬레이터 | `READY`만 버튼 활성화, `MISSING`/`FAILED`/`DISABLED`는 비활성화한다. |
| Today QT 캐시 | 응답에는 `cache.cacheStatus`, `servedQtDate`, `staleReason`, `nextRefreshAt`을 포함한다. |
| 닉네임 나눔 | 사용자가 명시적으로 공유한 묵상만 닉네임 스냅샷과 함께 노출하고 계정 식별 정보는 응답하지 않는다. |
| 찬양 | 운영자 큐레이션과 사용자 저장만 제공하며 AI 추천, 가사·음원 저장, 직접 YouTube URL 입력은 제외한다. |
| 관리자 | `/api/v1/admin/**`는 `ADMIN` 권한이 필요하고 데이터 변경은 감사 로그 대상이다. |
| 관리자 UI 플랫폼 | 관리자 UI는 사용자 Flutter 앱과 분리된 **별도 웹 프런트엔드**다. Flutter 앱에는 관리자 메뉴를 두지 않는다(`03_아키텍처_정의서.md` v1.2 §4.9 / §13.6). |
| `@`멘션 본문 자동 삽입 | F-03/F-16 노트 본문에서 `@책 장:절` 또는 `@책 장:절-절`로 다른 성경 구절을 인용 블록으로 즉시 삽입. 트리거 기호는 `@`만 사용. 클라이언트가 입력을 파싱해 `GET /api/v1/bible/verses?bookCode=&chapter=&verseFrom=&verseTo=` 호출(`07` §6.4.1, `04` §4.2.2). |
| 닉네임 7일 변경 잠금 | 닉네임은 변경 가능하되 한 번 변경하면 7일 동안 재변경 불가. 가입 시 첫 설정과 형식 오류·중복 재설정은 잠금 대상 아님. 잠금 위반은 `409 NICKNAME_CHANGE_LOCKED`로 거절하며 응답에 `nicknameUnlockAt` 포함(`07` §F-10 닉네임 정책, `04` §4.1.5, `02` `members.nickname_last_changed_at`). |
| 성경 데이터 저장 위치 | 한글 성경 본문은 **클라이언트 로컬 SQLite + 서버 마스터 사본** (오프라인 표시 지원), 영어 성경 본문은 **서버 마스터 + 온라인 조회**. 한글은 앱 첫 실행 시 한글 번들 다운로드 API로 적재(`07` §F-01 §데이터 정책, `04` §4.2 한글 번들 다운로드, `02` `bible_books`/`bible_verses` 서버 마스터). |
| 저작권 표현 | “저작권 문제 없음”이 아니라 “저작권 리스크를 낮춘다”로 표현한다. |

---

## 3. 기능 상세

| F-ID | 기능 | 주요 동작 | 진입점 | 예외·비활성 조건 | 완료 기준 |
| --- | --- | --- | --- | --- | --- |
| F-01 | 성경 묵상 화면 | Today QT 본문, 한/영 본문, 해설 진입점, 묵상 진입점, 시뮬레이터 상태, 캐시 상태를 조립한다. 한글 성경은 클라이언트 로컬 SQLite에서 조회(오프라인 가능), 영어 성경은 서버 온라인 조회 | `GET /api/v1/qt/today`, `GET /api/v1/bible/verses`(영어), `GET /api/v1/bible/bundle?language=ko&version=`(한글 번들 첫 적재) | 00:00~04:00은 `STALE_FALLBACK`으로 이전 캐시 제공 가능. 캐시가 전혀 없으면 `EMPTY` 준비 중 응답. 오프라인이면 한글 본문은 로컬에서 표시, 영어 본문은 사용 불가 안내. 한글 로컬 데이터 미다운로드 시 별도 안내 | Today QT 응답이 본문·해설 진입점·묵상 진입점·시뮬레이터 상태와 `cache.cacheStatus`를 항상 반환하고, 한글 본문은 오프라인에서도 표시됨 |
| F-02 | AI 콘텐츠 사전 생성 | `commentary_materials` 입력으로 검증 가능한 해설 산출물을 만들고 승인 후 `verse_explanations`에 노출한다. 시뮬레이터 산출물도 사전 생성한다. | 04:00 KST batch, `POST /api/v1/system/ai/generation-jobs`, `POST /api/v1/admin/ai/assets/{assetId}/regenerate` | 해설·시뮬레이터는 사용자 요청 시 생성 금지. 검증 참조 원문 사용자 노출 금지. 실패는 로그와 재처리 가능 상태로 남김 | 승인 해설 또는 실패 로그가 기록되고, Today QT 캐시와 `ai_generation_jobs`/`ai_generated_assets` 상태가 갱신됨 |
| F-03 | 묵상 노트 | 오늘 QT 기준 DRAFT 생성/조회, 목록·상세 조회, 수정, 삭제, 이벤트 기록, 달력 반영. 본문 안에서 `@`멘션으로 다른 성경 구절을 인용 블록으로 즉시 삽입 (`07` §6.4.1) | `GET /api/v1/notes/draft`, `POST /api/v1/notes`, `GET /api/v1/notes`, `GET/PATCH/DELETE /api/v1/notes/{noteId}`, `GET /api/v1/note-categories`, `GET /api/v1/me/meditation-calendar`. `@`멘션 시 `GET /api/v1/bible/verses?bookCode=&chapter=&verseFrom=&verseTo=` 호출(클라이언트가 입력 파싱) | 미인증 사용자는 저장 불가. 자동 저장 불가. 잘못된 책 이름·장·절 입력은 인용 블록 삽입 없이 안내만 표시 | 4개 섹션 중 1개 이상 입력 시 저장 가능, 변경 이벤트와 달력 상태가 기록됨. `@`멘션으로 삽입된 절은 `note_verses`에 중복 없이 기록됨 |
| F-04 | 회원 인증 | Kakao OAuth, JWT 발급·검증, Refresh Token 관리. 카카오 로그인 직후 닉네임 없으면 닉네임 설정 화면(A-03)으로 이동(카카오 닉네임 기본값) — 설정해야 메인 진입 가능. 닉네임 변경은 7일 잠금 정책(F-10) 적용 | `GET /oauth2/authorization/kakao`, `GET /oauth2/callback/kakao`, `POST /api/v1/auth/token/refresh`, `POST /api/v1/auth/logout`, `GET /api/v1/me`, `PATCH /api/v1/me/profile`(닉네임 수정, 7일 잠금) | 교회 인증 버튼/API 없음. 닉네임 잠금 기간(7일) 안에 변경 시 `409 NICKNAME_CHANGE_LOCKED` | 토큰 재발급·로그아웃·세션 조회·닉네임 설정·변경이 API 명세와 맞게 동작하고, 7일 잠금이 정확히 계산됨 |
| F-05 | 사용자 알림 | 이벤트 기반 알림 생성 후 인앱 알림 목록에서 조회 | `GET /api/v1/notifications`, `PATCH /api/v1/notifications/{notificationId}/read` | WebSocket/STOMP는 v1 제외 | 로그인 사용자가 알림 목록과 읽음 상태를 확인 가능 |
| F-06 | 관리자 운영 | QT 범위, AI 산출물, 신고, 찬양 큐레이션, 공지, 감사 로그 관리. UI는 사용자 Flutter 앱과 분리된 **별도 관리자 웹 프런트엔드**에서 제공 | `/api/v1/admin/dashboard`, `/api/v1/admin/qt-passages/**`, `/api/v1/admin/ai/**`, `/api/v1/admin/reports/**`, `/api/v1/admin/praise-songs/**`, `/api/v1/admin/notices/**`, `/api/v1/admin/audit-logs` (관리자 웹에서 호출) | 권한별 ADMIN/OPERATOR/REVIEWER/CONTENT_CREATOR 기준 적용. 관리자 변경은 감사 로그 필수. 사용자 Flutter 앱에 관리자 화면 포함 금지 | 관리자 주요 작업이 권한과 audit log 기준을 만족하며, 사용자 앱과 관리자 웹 둘 다 같은 `qtai-server`를 호출 |
| F-07 | 첫 실행 튜토리얼 | QT 의미와 앱 사용 순서를 안내하고 Today QT로 진입 | 앱 첫 실행 | 로그인 wall로 막지 않음 | 튜토리얼 종료 후 Today QT 화면으로 이동 |
| F-08 | 쉬운 본문 요약·해설·용어 풀이 | 승인된 `verse_explanations`와 `glossary_terms`를 선택 열람 형태로 제공 | `GET /api/v1/qt/{qtPassageId}/study-content` | 승인되지 않은 산출물과 검증 참조 원문 노출 금지 | 승인 해설과 출처 정보만 표시 |
| F-09 | 찬양 큐레이션·내 찬양 목록 | 운영자 큐레이션 곡 조회, 사용자 저장/제거 | `GET /api/v1/praise-songs`, `GET /api/v1/me/praise-songs`, `POST /api/v1/me/praise-songs`, `DELETE /api/v1/me/praise-songs/{id}` | AI 추천 금지, 가사·음원 저장 금지, 직접 URL 입력 제외 | 큐레이션/내 목록/저장/삭제가 `praise_songs`, `member_praise_songs` 기준으로 동작 |
| F-10 | 닉네임 나눔 | 사용자가 선택한 묵상만 닉네임 스냅샷