# 코드리뷰 후속 TODO — 이지윤 (DevA · bible/qt 도메인)

- 출처: `doc/2026-06-10_서버_코드리뷰.md` §담당 배정
- 공통 규칙: 브랜치 `{type}/{scope}-…`(dev에서 분기), PR 대상 `dev`, 10파일/500라인 이하 (CLAUDE.md §12)

## 우선순위 요약

| # | 항목 | 심각도 | 예상 규모 |
|---|---|---|---|
| 1 | QtVisibility 빈 enum 정리 | P3 | 파일 1개 |
| 2 | SU 수집 title 저장 최소화 검토 | P3 | 정책 검토 → 소규모 |
| 3 | (리뷰어) 배치 이중 실행 PR — SU 수집 측 검증 | P1 지원 | 리뷰 |
| 4 | (페어) AD-02 응답 계약 확정 → 김지민 전달 | P2 지원 | 계약 문서 |

---

## TODO 1. [P3] QtVisibility 빈 enum 정리

**문제**: `service-bible/src/main/java/com/qtai/domain/qt/internal/QtVisibility.java`가 값 없는 빈 enum + `// TODO: PRIVATE, PUBLIC` 상태. 사용처가 없어 혼란만 유발(빈 enum은 컴파일은 되지만 인스턴스가 존재할 수 없는 타입).

**해결책**: 사용 계획이 있으면 값을 채우고, 없으면 삭제. 현재 사용처 0건이므로 **삭제 권장**.

- [ ] 사용처 재확인: 리포지토리 루트에서 `QtVisibility` 전체 검색(코드·테스트·문서) — 리뷰 시점 기준 선언부 외 0건
- [ ] 같은 이름이 admin-server 복사본에도 있는지 확인 후 함께 삭제 (`admin-server/.../qt/internal/QtVisibility.java` 존재 시 동일 PR에서)
- [ ] 만약 요구사항(07 정의서)에 노트/QT 공개범위 기능이 살아 있으면 삭제 대신 값 채움 — 이 경우 ERD(02)와 컬럼 매핑까지 같이 확인하고 Lead 검토 요청

**검증**: `./gradlew -p qtai-server :service-bible:build` 통과(참조 없으므로 컴파일 에러 없음).

## TODO 2. [P3] SU 수집 title 저장 최소화 검토

**문제**: 성서유니온 수집(`SuTodayPassage`)은 본문 텍스트를 저장하지 않아 CLAUDE.md §8을 준수하지만, `title`(SU가 붙인 글 제목)은 저장 중. 제목도 외부 저작물의 일부라 저장하지 않으면 저작권 리스크를 더 낮출 수 있음.

**해결책(검토 순서)**:

1. 현재 `title`이 사용자 노출 어디에 쓰이는지 추적(`qt_passages` 테이블 → API 응답 → 앱 화면).
2. 노출이 필수가 아니면: 수집 시 title 저장 생략(컬럼 nullable 유지) 또는 "책 장:절" 형태 자체 생성 문자열로 대체.
3. 노출이 필요하면: 현행 유지하되 리스크 메모를 문서에 남기고 Lead 판단 요청.

- [ ] title 사용처 추적 및 위 1~3 중 방향 결정(소속 도메인 오너로서 제안서 형태면 충분)
- [ ] 변경 시 마이그레이션 불필요(저장 생략이면 코드만) — `QtTodayPassageImportService.updateExisting/createNew` 수정
- [ ] 표현 주의: 문서·PR에 "저작권 문제 없음"이 아니라 "저작권 리스크를 낮춘다"로 기술 (CLAUDE.md §8)

**검증**: 수집 단위 테스트 + 오늘의 QT API 응답에서 제목 표시 확인.

## TODO 3. [P1 지원] 배치 이중 실행 PR 리뷰 (SU 수집 측)

**배경**: 강태오가 admin-server의 SU 수집 토글을 기본 false로 내리는 PR을 올림. SU 수집 도메인 오너로서 검증.

- [ ] service-bible 쪽 수집이 단독 실행돼도 기능 누락이 없는지 확인(00:02 cron + startup 보강 `importTodayOnStartup` 모두 service-bible에 존재)
- [ ] admin-server에서 수동/관리자 트리거로 수집을 다시 돌릴 운영 경로가 필요한지 의견 제시(필요 시 별도 admin API로 — 사용자 요청 경로 즉시 생성 금지 원칙과는 무관)

## TODO 4. [P2 지원] AD-02(QT 관리) 응답 계약 확정 → 김지민

**배경**: admin-web AD-02 화면이 임시 DTO(`[key: string]: unknown`)로 대기 중. 본인이 만든 admin qt-passages API(#454)의 응답 스키마를 확정해 전달해야 함.

- [ ] `2026-06-10_admin-qt-passages-contract-to-jimin.md`(본인 workspaces) 기준으로 최종 필드 목록·타입·페이지네이션 형태 확정
- [ ] 김지민에게 전달 후 admin-web 타입 반영 PR 리뷰 참여

**검증**: admin-web AD-02 화면이 실데이터로 렌더링되는지 김지민과 공동 확인.
