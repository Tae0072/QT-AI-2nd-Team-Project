# 코드리뷰 후속 TODO — 강상민 (DevC · AI 도메인)

- 출처: `doc/2026-06-10_서버_코드리뷰.md` · `…관리자웹_코드리뷰.md` §담당 배정
- 공통 규칙: 브랜치 `{type}/{scope}-…`(dev에서 분기), PR 대상 `dev`, 10파일/500라인 이하 (CLAUDE.md §12)

## 우선순위 요약

| # | 항목 | 심각도 | 예상 규모 |
|---|---|---|---|
| 1 | service-ai의 봉인된 Admin 컨트롤러 4종 정리 | P3 | 파일 4개± 삭제 |
| 2 | (페어) AD-01/06 응답 계약 확정 → 김지민 전달 | P2 지원 | 계약 문서 |
| 3 | (리뷰어) 배치 이중 실행 PR — AI 시딩 측 검증 | P1 지원 | 리뷰 |
| 4 | F-15 Q&A 구현 시 §7 차단 로직 (예정 작업 가이드) | 예정 | 본 구현 |

---

## TODO 1. [P3] service-ai의 봉인된 Admin AI 컨트롤러 4종 정리

**문제**: service-ai에 `AdminAiAssetController`·`AdminAiMonitoringController`·`AdminAiBatchRunLogController`·`AdminAiValidationChecklistController`가 admin-server와 **동일 경로**(`/api/v1/admin/ai/**`)로 존재한다. service-ai `SecurityConfig:50`이 `/api/v1/admin/**`를 denyAll로 봉인해 보안 문제는 없지만(사유 주석도 있음), 영원히 호출 불가능한 죽은 코드라 혼란을 유발한다. admin-web이 실제로 쓰는 것은 admin-server 쪽 사본이다.

**해결책**: service-ai에서 4개 컨트롤러(+이들만 쓰는 web DTO) 삭제. denyAll 규칙과 그 주석은 "재유입 방지 가드"로 **유지**.

- [ ] 삭제 전 의존 확인: 4개 컨트롤러를 참조하는 테스트(`AiForbiddenFeatureTest`는 컨트롤러 전수 스캔 방식이라 삭제에 영향 없음 — 확인만) 및 web DTO 사용처 검색
- [ ] `service-ai/.../domain/ai/web/AdminAi*.java` 4종 삭제, 이들 전용 Request/Response DTO도 같이(공용이면 잔류)
- [ ] SecurityConfig의 denyAll과 javadoc은 유지(왜 막는지 설명이 이미 좋음)
- [ ] PR 본문: "admin-web 호출 경로는 admin-server(8090) — 리뷰 문서 §P3 근거" 링크
- [ ] 커밋 예: `chore(ai): denyAll로 봉인된 admin 컨트롤러 사본 제거 (admin-server가 단독 제공)`

**검증**: `./gradlew -p qtai-server :service-ai:build test` 통과 + `AiForbiddenFeatureTest` 그린.

## TODO 2. [P2 지원] AD-01(대시보드)·AD-06(공지) 응답 계약 확정 → 김지민

**배경**: admin-web의 AD-01/06 화면이 임시 DTO(`[key: string]: unknown`)로 "백엔드 준비 중" 상태. 본인이 만든 admin-dashboard API(#449)·admin-notices API(#450)의 응답 스키마를 확정해 전달해야 화면이 완성된다.

- [ ] 두 API의 최종 응답 필드·타입·페이지네이션(공통 `Page` 봉투 여부) 확정 — 기존 `ApiResponse<T>` 규칙 유지
- [ ] 계약 문서를 본인 workspaces에 작성(이승욱의 `2026-06-10_admin-kakao-auth-api-contract.md` 형식 참고) 후 김지민에게 전달
- [ ] 김지민의 admin-web 타입 반영 PR 리뷰 참여

**검증**: AD-01/06 화면 실데이터 렌더링 공동 확인.

## TODO 3. [P1 지원] 배치 이중 실행 PR 리뷰 (AI 시딩 측)

**배경**: 강태오가 admin-server의 `ai.daily-qt-verse-seed.enabled` 기본 false PR을 올림. AI 도메인 오너로서 검증.

- [ ] service-ai 단독 실행으로 00:05 시딩이 완결되는지 확인(시딩 전제: 00:02 SU 수집 — service-bible 단독 실행과 시간 순서 유지됨)
- [ ] admin-server의 `AiBatchMonitoringService` 기록 경로가 시딩 비활성화 후에도 service-ai발 기록을 정상 수신하는지 확인(`WriteAuditLog`/배치 런 로그 흐름)
- [ ] 듀얼 실행 시절의 `AiBatchRunLog` 이중 기록이 대시보드(AD-01)·모니터링(AD-08) 집계에 남긴 왜곡이 있는지 확인 — 있으면 화면 주석/필터로 처리 제안

## TODO 4. [예정] F-15 Q&A 구현 가이드 (착수 시 본 가이드 따름)

**배경**: 사용자용 Q&A는 현재 placeholder(`service-ai/.../web/AiController.java` — 빈 클래스, DTO `RequestAiQaCommand`만 존재). 구현 시 CLAUDE.md §7이 그대로 적용된다.

**구현 시 필수 체크리스트**:

- [ ] 엔드포인트는 placeholder 주석대로 `POST /api/v1/ai/qa-requests`(생성) + `GET /api/v1/ai/qa-requests/{id}`(폴링) — 동기 스트리밍/SSE 금지(§8)
- [ ] **single-turn 강제**: 요청에 이전 대화/세션 식별자 입력란 자체를 두지 않음
- [ ] **주제 제한**: 단어·시대상·역사적 배경만 허용 — 1차 키워드/패턴 차단(가치판단·신앙평가·상담·설교 요청) + 차단 사유는 ErrorCode로 반환, 차단 로그는 `ai_validation_logs`에 기록
- [ ] **원문 미반환**: DeepSeek 응답은 검증 통과 전 사용자에게 절대 미노출 — job 상태(PENDING/VALIDATED/REJECTED)로만 폴링 응답
- [ ] 기록: `ai_generation_jobs`/`ai_generated_assets`/`ai_validation_logs`에 prompt/model/hash/status/검증 체크리스트 버전 기록(§7)
- [ ] 테스트(§10): 차단 케이스·검증 실패 처리·미승인 미노출·`AiForbiddenFeatureTest` 경로 통과 전부 필수
- [ ] 착수 전 요구사항(07 정의서 F-15) 재확인 — 범위가 흔들리면 Lead 검토 표시
