## W2 -- 지금 바로 할 작업

목표: 실제로 관리자/배치 경로에서만 AI 생성 Job을 만들 수 있게 한다.

갱신 기준: 2026-05-22 현재 구현과 DevC report 기준.

| 순서 | 작업                                     | 상태                                                                 |
| ---: | ---------------------------------------- | -------------------------------------------------------------------- |
|    1 | 관리자/시스템 AI 생성 트리거 API 만들기  | 완료 - 관리자 재생성 접수 API와 시스템 배치 생성 job 트리거 API 구현 |
|    2 | `CreateAiGenerationJobUseCase` 실제 구현 | 완료 - `AiService` 구현과 단위 테스트 반영                           |
|    3 | 중복 실행 방지 구현                      | 부분 완료 - `QUEUED`/`RUNNING` 차단 구현, `inputHash` 저장 정책 후속 |
|    4 | `ai_generated_assets` 등록 흐름 구현     | 부분 기반 있음 - Entity/Repository/내부 `AiLogService` 있음, UseCase 구현 미연결 |
|    5 | `ai_validation_logs` 등록 흐름 구현      | 부분 기반 있음 - Entity/Repository/내부 `AiLogService` 있음, UseCase 구현 미연결 |
|    6 | 관리자 재생성 가능 상태 판단             | 완료 - `REJECTED`/`HIDDEN` 허용, `VALIDATING`/`APPROVED` 차단         |
|    7 | 관리자 로그 조회 API 초안                | 미착수 - 산출물/검증 로그 UseCase 연결 후 진행 권장                  |

다음 작업: `POST /api/v1/system/ai/assets` 산출물 등록 API 또는 `POST /api/v1/system/ai/validation-logs` 검증 로그 등록 API 구현.
