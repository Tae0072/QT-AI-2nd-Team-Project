# 코드리뷰 후속 TODO — 김지민 (DevE · admin-web FE / note·sharing)

- 출처: `doc/2026-06-10_관리자웹_코드리뷰.md` · `…플러터_코드리뷰.md` §담당 배정
- 공통 규칙: 브랜치 `{type}/{scope}-…`(dev에서 분기), PR 대상 `dev`, 10파일/500라인 이하 (CLAUDE.md §12)

> **✅ 완료 (2026-06-11):** TODO 1~5 전부 dev 머지 완료. 찬양 숨김(hide)만 v1 의도적 제외(v1.1 보류).
> | TODO | PR |
> |---|---|
> | 1 로그인 라우팅 vite proxy | #482 |
> | 2 refreshToken 갱신 | #504 |
> | 3 AD-01 / AD-02 / AD-06 DTO | #490 / #496 / #501 |
> | 4 토큰 보관 재검토(주석+HttpOnly 전환계획) | #517 |
> | 5 SDK 안내 / code-split | #512 / #513 |
> | (5 대체) 찬양 수정·삭제 CRUD + 리뷰 4건 | #525 |

## 우선순위 요약

| # | 항목 | 심각도 | 예상 규모 |
|---|---|---|---|
| 1 | 로그인 라우팅 수정 (vite proxy 1룰) | P1 | 설정 1파일 |
| 2 | refreshToken 갱신 흐름 추가 | P2 | client.ts 중심 |
| 3 | AD-01/02/06 임시 DTO 구체화 | P2 | 타입+화면 3건 |
| 4 | localStorage 토큰 보관 재검토 | P2 | 결정+주석 |
| 5 | P3 3건 (찬양 숨김 버튼·SDK 안내·code-split) | P3 | 소규모 |

---

## TODO 1. [P1] 로그인 라우팅 수정 — vite proxy 룰 추가

**문제**: 로그인 `POST /api/v1/admin/auth/kakao`는 service-user(8081)에만 있는데, 현재 vite proxy는 모든 `/api`를 admin-server(8090)로 보낸다(`vite.config.ts:14,20-25`). 8090엔 그 컨트롤러가 없고 `/api/v1/admin/**`은 ADMIN 토큰을 요구해 **카카오 키를 넣어도 로그인이 401로 실패**한다. (`LoginPage.tsx:21` 주석의 "백엔드 준비 전"은 사실과 다름 — 서버는 #452로 완료, 라우팅이 원인)

**해결책**: 구체 경로 룰을 `'/api'`보다 **위에** 추가(http-proxy는 정의 순서대로 매칭).

```ts
// vite.config.ts
const proxyTarget = env.VITE_API_PROXY_TARGET ?? 'http://localhost:8090';
const authProxyTarget = env.VITE_AUTH_PROXY_TARGET ?? 'http://localhost:8081';

proxy: {
  // 관리자 카카오 로그인(+추후 refresh)은 service-user 소관 — 8081로
  '/api/v1/admin/auth': { target: authProxyTarget, changeOrigin: true },
  '/api': { target: proxyTarget, changeOrigin: true },
},
```

- [x] 위 룰 추가 + `.env.example`에 `VITE_AUTH_PROXY_TARGET` 항목·설명 추가 (#482)
- [x] `LoginPage.tsx:21` 주석 정정("라우팅 연결 완료, 카카오 JS 키만 주입하면 실로그인 가능"으로) (#482)
- [x] 이승욱과 합동 확인: dev에서 로그인 시 service-user 로그에 요청 도달, 비관리자 403 `ADMIN_USER_NOT_FOUND`·관리자 토큰 발급 확인
- [x] 참고: 강태오의 nginx 통합안이 확정되면 prod는 nginx가 같은 규칙을 가짐 — 이 vite 룰은 dev 전용으로 그대로 공존 가능
- [x] 커밋 예: `fix(admin-web): 관리자 인증 경로를 service-user(8081)로 프록시 — 실로그인 복구` (#482 머지)

**검증**: dev 서버에서 카카오 로그인 E2E 1회(또는 dev 토큰 경로와 병행 확인).

## TODO 2. [P2] refreshToken 갱신 흐름 추가

**문제**: 로그인 응답의 `refreshToken`(`src/api/adminAuth.ts:13`)을 받기만 하고 안 씀. 401이면 `clearToken()` 후 재로그인 유도(`client.ts:44-46`) → access 30분 정책상 **30분마다 강제 재로그인**.

**해결책**: ① 이승욱과 refresh 엔드포인트 확정(그의 TODO 5에 계약 항목 있음) → ② axios 응답 인터셉터에 single-flight 갱신 구현.

```ts
// client.ts 스케치 — 동시 다발 401에 refresh 1회만 (flutter의 single-flight 패턴과 동일 사상)
let refreshing: Promise<string> | null = null;

async function refreshOnce(): Promise<string> {
  refreshing ??= axios
    .post(`${API_BASE_URL}/admin/auth/refresh`, { refreshToken: getRefreshToken() })
    .then((r) => { saveTokens(r.data.data); return r.data.data.accessToken; })
    .finally(() => { refreshing = null; });
  return refreshing;
}
// 응답 인터셉터: status===401 && !config._retried && refresh 보유 시
//   → config._retried = true; 새 토큰으로 원요청 1회 재시도. 실패 시 clearToken().
```

- [x] tokenStorage에 refreshToken 저장/삭제 추가(로그아웃 시 함께 제거) (#504)
- [x] `/admin/auth/**` 요청 자체의 401은 재시도 제외(무한루프 방지) (#504)
- [x] 엔드포인트 경로는 이승욱 계약 확정값 사용 — TODO 1의 8081 프록시 룰이 `/api/v1/admin/auth` prefix라 refresh도 같은 prefix면 추가 설정 불필요
- [x] 간단 테스트(vitest 또는 수동): 401→갱신→재시도 성공 / refresh 실패→로그인 화면 (#504)

**검증**: access 만료 시나리오(서버에서 짧은 만료로 발급받아 테스트)에서 화면 유지 확인.

## TODO 3. [P2] AD-01/02/06 임시 DTO 구체화

**문제**: 대시보드(AD-01)·QT 관리(AD-02)·공지(AD-06) 응답 타입이 `[key: string]: unknown` 임시 형태.

**해결책**: 백엔드 오너의 계약 확정을 받아 타입 교체.

- [x] AD-01·AD-06 — 강상민의 계약 문서 수신 후 타입 작성(dashboard #449, notices #450) → FE #490 / #501
- [x] AD-02 — 이지윤의 계약 문서 수신(`2026-06-10_admin-qt-passages-contract-to-jimin.md` 갱신본) 후 타입 작성 → FE #496
- [x] `[key: string]: unknown` 제거, "백엔드 준비 중" Alert 철거, 실데이터 렌더링 + 빈 상태/에러 상태 처리 (#490/#496/#501)
- [x] 화면당 PR 분리 권장(500라인 제한 준수 용이) — AD-01/02/06 각각 별도 PR로 분리 완료

**검증**: 각 화면 실데이터 렌더링을 해당 백엔드 오너와 공동 확인.

## TODO 4. [P2] localStorage 토큰 보관 재검토

**문제**: ADMIN 토큰을 localStorage에 저장 — XSS 발생 시 토큰 탈취 가능. 의도적 임시 설계(주석 있음)지만 "졸업 시점"이 정해지지 않음.

**해결책(단계)**:

1. 지금: 위험 인지 주석 보강 + 운영 전 전환 계획을 본 TODO에 기록(완료 조건 명시).
2. 운영 전: HttpOnly 쿠키 방식 전환 검토 — 서버(CORS credentials, 쿠키 발급) 변경이 필요하므로 이승욱·강태오와 함께 결정. refresh(TODO 2)와 묶으면 변경 1회로 끝남.

- [x] 강태오에게 전환 시점(데모 전/후) 결정 요청 후 결과를 여기 기록 — **데모 후 HttpOnly 전환** 결정, 위험 인지 주석 보강 (#517)

## TODO 5. [P3] 소규모 3건

- [ ] ⏸️ **찬양 숨김 버튼 연결**: **v1 의도적 제외(v1.1 보류).** hide(`POST /{id}/hide`)는 v1 범위 외 → 대신 수정/삭제 CRUD를 #525로 구현. `PraiseSong.hide()` 엔티티 메서드는 이미 존재해 v1.1에서 컨트롤러 엔드포인트 + `hidePraiseSong` 행 액션만 추가하면 됨
- [x] **카카오 SDK 로드 실패 안내**: `src/auth/kakao.ts:33`의 키 미설정 에러를 LoginPage에서 antd `Alert`로 노출 + 로그인 버튼 비활성화 (#512)
- [x] **code-split**: 1.15MB 단일 청크 → `vite.config.ts` `manualChunks`(antd/vendor) 분리 (#513)

**검증**: `npm run build` 통과 + 청크 분리 확인, 화면 동작 스모크.

## (예정) flutter 신고 화면 — 서버 report 도메인 오너로서

flutter-app에 신고 UI가 없음(서버 `service-note` report는 구현됨). 앱 신고 화면 착수 시 본인이 서버 계약(제출 API)을 제공하고 화면은 flutter 담당과 페어로 진행 — 착수 시점은 Lead 일정 결정 따름.
