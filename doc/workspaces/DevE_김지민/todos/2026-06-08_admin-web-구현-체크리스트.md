# 2026-06-08 · 관리자 웹(admin-web) 구현 체크리스트 (김지민)

> 진행 추적용. 전략·실행 절차는 [구현 로드맵 + 워크플로우](../workflows/2026-06-08_admin-web-구현-로드맵.md), 화면별 API·기능은 [화면별 명세](../study-notes/2026-06-08_admin-web-화면별-명세.md) 참조.
> 표기: `[ ]` 미완료 · `[x]` 완료 · `[~]` 진행 중
>
> **⚠️ 2026-06-08 방향 전환**: 팀이 MSA 분리 작업 중 → PR/머지 없이 각자 **push만**. **AD-07 감사로그·AD-03 AI검증 = 팀원 담당으로 이관**. 김지민 현재 = **AD-05 찬양 큐레이션** (브랜치 `feature/admin-web-praise-songs`). (이전: AD-04 신고처리·AD-08 AI모니터링 완료)

---

## ▶ 현재 작업 — AD-05 찬양 큐레이션 (브랜치 `feature/admin-web-praise-songs`)

> dev에서 깨끗하게 분기(2026-06-09). 작업추적 문서 3종은 ai-monitoring 브랜치에서 이관.
> ⚠️ **C0 재추가 필요**: dev엔 `useList`·antd `<App>` 래퍼가 아직 머지 전이라 **이 브랜치에서 다시 만든다**(MSA — 중복은 나중에 합침). AD-04(reports)·AD-08에서 만든 것과 동일.
> ⚠️ **백엔드 부분 구현**: 04 §4.7.6 기준 `GET`(목록)·`POST`(등록)만 ✅ / `PATCH`(수정)·`POST .../{id}/hide`(숨김)는 **백엔드 미구현 → 이번엔 목록+등록만**, 수정/숨김은 백엔드 선행 후.
> 🚫 **금지(CLAUDE.md §8 / 07 F-09)**: 가사·음원 파일·직접 YouTube URL **저장 금지**. 곡 **메타데이터만**(곡명·아티스트·출처(CURATED/DEVICE)·라이선스 메모·상태).

### C0 공통 (이 브랜치에서 재추가)
- [x] `src/main.tsx` — antd `<App>` 래퍼(`App as AntApp`) → 토스트 컨텍스트 (reports 브랜치에서 `git checkout`으로 가져옴)
- [x] `src/hooks/useList.ts` — 목록 공통 훅(data·rows·loading·params·setParams·reload, 에러 토스트). AD-04/AD-07 것과 동일 (reports 브랜치에서 그대로 가져옴)

### AD-05 (구현 완료)
- [x] `src/api/praiseSongs.ts` — `PraiseSong` 타입 구체화(`title`·`artist`·`sourceType`(CURATED|DEVICE)·`licenseNote`·`status`(ACTIVE|HIDDEN)·`createdAt`·`updatedAt`) + `createPraiseSong` 등록 함수 추가. (가사·음원·URL 필드 없음)
- [x] `src/pages/PraiseSongsPage.tsx` — PagePlaceholder 제거 → 목록(`Table`+`useList`)+필터(status) + **등록 폼 모달**(곡명·아티스트·출처·라이선스 메모·상태 → POST 후 `reload()`). 🚫 가사·음원·URL 입력란 없음
- [x] `npm run typecheck` + `npm run build` 통과 (antd 청크 500kB↑ 경고는 비차단)
- [ ] 커밋 → push (MSA — PR 없음)
- [ ] (선택) 로컬 확인: ADMIN 토큰 → `/praise-songs` 목록·필터·등록 동작
- API: `GET /api/v1/admin/praise-songs`·`POST /api/v1/admin/praise-songs` (04 §4.7.6) · 권한 OPERATOR. (수정/숨김은 백엔드 대기)

---

## (완료) AD-08 AI 운영 모니터링 (브랜치 `feature/admin-web-ai-monitoring`)

> dev에서 분기(2026-06-08). 어제 만든 기획 문서 3종 + admin-web 기초 학습노트 4부작을 이 브랜치로 가져옴.
> ✅ **2026-06-09 결정 확정 후 구현 완료.**
>   - 선택 1(조회 방식): **A** — 페이지 안 `useState`(data·loading) + `useEffect` 직접 호출. (단건 화면이라 useList/useAsync 안 씀)
>   - 선택 2(집계 표시): **카드형(안 A)** — Card + Statistic 그룹(생성잡·검증·Q&A) + 사유/통과율 표. (표형 대신 큰 숫자 강조)
>   - 선택 3(기간 기본값): **최근 7일** — 하루 1배치(00:05) 운영 한눈 파악 관점. RangePicker라 한 달·임의 기간은 바로 변경 가능.
> 참고: AD-08은 **목록이 아니라 집계 단건** → `useList` 안 씀(AD-04와 다른 핵심).

### AD-08 (완료)
- [x] `src/main.tsx` — antd `<App>` 래퍼 추가(`App as AntApp`) → 토스트 컨텍스트 (이 브랜치엔 C0가 없어 함께 도입)
- [x] `src/api/aiMonitoring.ts` — `AiMonitoringSummary` 타입 구체화(`period`·`generationJobs`·`validation`·`qa`·`checklists` + 사유/체크리스트 하위 타입)
- [x] `src/pages/AiMonitoringPage.tsx` — PagePlaceholder 제거 → 기간 필터(RangePicker, 기본 최근 7일) + 집계 카드(Statistic) + 검증 실패/Q&A 차단 사유 표 + 체크리스트 통과율 표
- [x] `npm run typecheck` 무오류 + `npm run build` 성공 (antd 청크 500kB↑ 경고는 비차단)
- [x] 로컬 확인 완료(2026-06-09): `/ai-monitoring` 기간 조회·카드·사유 표 동작 OK
- [x] 커밋 (MSA — PR 없음, 커밋 `7031c02`)
- [~] push — 김지민 직접 진행(dev 반영 포함)
- API: `GET /api/v1/admin/ai/monitoring?from=&to=` (04 §4.7.9) · 권한 OPERATOR(집계만)

---

## (완료·push됨) AD-04 신고 처리 (브랜치 `feature/admin-web-reports`)

### C0 공통 (PR1에서 만듦 → 이 브랜치로 가져옴, 재사용)
- [x] `src/main.tsx` antd `<App>` 래퍼 + `src/hooks/useList.ts`

### AD-04 신고 처리
- [x] `src/api/reports.ts` — `Report` 타입 구체화(targetType·reason·status·detail·processedAt 등)
- [x] `src/pages/ReportsPage.tsx` — PagePlaceholder 제거
  - [x] 필터(status·targetType `Select`) + `Table`(useList) + 페이징
  - [x] **처리 모달**(resolve: action·reason·notifyReporter / reject) → API 후 `reload()`. RECEIVED만 액션 노출
- [x] `npm run typecheck` + `npm run build` 통과
- [~] 커밋 → **push만**(PR 없음 — MSA)
- [ ] (선택) 로컬 확인: dev #345 관리자 토큰 → `/reports` 필터·표·처리 모달 동작

> 화면 확인 절차·토큰 트러블슈팅은 아래 (기록) AD-07 섹션의 🖥️ 부분과 동일하게 적용.

---

## (기록·팀원 이관) PR1 — C0 공통 + AD-07 감사 로그 (브랜치 `feature/admin-web-audit-log`)

### 셋업 (A2~A4)
- [x] dev 최신화 + `feature/admin-web-audit-log` 분기(origin/dev)
- [x] `cd admin-web && npm install` (node_modules)
- [x] `npm run dev`(5173) 로컬 기동 확인

### C0 공통 인프라
- [x] `src/main.tsx` — antd `<App>` 래퍼 추가(`App as AntApp`, ConfigProvider 안쪽) → 토스트 컨텍스트
- [x] `src/hooks/useList.ts`(신규) — `useList<T,P>(fetcher, initialParams)`: data·rows·loading·params·setParams·reload, 에러 토스트
- [ ] (선택) `src/components/StatusTag.tsx` — 상태값→Tag 색 매핑 (AD-04부터 필요 시, 이번 PR 미포함)

### C1 AD-07 감사 로그
- [x] `src/api/auditLogs.ts` — `AuditLog` 타입 실제 필드로 구체화(id·actorType·actorLabel·actionType·targetType·targetId·createdAt 등)
- [x] `src/pages/AuditLogsPage.tsx` — PagePlaceholder 제거
  - [x] 필터 폼(antd Form, actorType·actionType·from~to RangePicker → `YYYY-MM-DD`)
  - [x] antd `Table`(rowKey=id, 컬럼: 시각·주체·행위·대상)
  - [x] 페이징(0-based↔1-based, total=totalElements) + `useList` 연동
  - [x] loading·에러 토스트 연동

### 검증·마무리
- [x] `npm run typecheck` 무오류
- [x] `npm run build` 성공 (antd 청크 500kB↑ 경고는 비차단)
- [~] 로컬: ADMIN 토큰 입력 → `/audit-logs` 동작 확인 (진행 중 — 아래 🖥️ 참조)
- [ ] 커밋(docs + feat) → push → PR(base dev, F-06)

---

### 🖥️ 화면 확인 (로컬 QA) — 나중에 다시 볼 때

**이번에 만든 것 (파일별):**
- `src/main.tsx` — 토스트(알림) 켜는 antd `<App>` 래퍼
- `src/hooks/useList.ts` — 목록 공통 도구(데이터·로딩·필터·페이징, 에러 토스트). **다음 화면들도 재사용**
- `src/api/auditLogs.ts` — 감사로그 데이터 가져오는 함수 + 타입
- `src/pages/AuditLogsPage.tsx` — **AD-07 화면**: 필터 폼 + 표 + 페이지네이션

**확인 절차:**
1. `npm run dev` → 브라우저 `localhost:5173`
2. 로그인 화면에 ADMIN 토큰 붙여넣기(**`Bearer ` 빼고 `eyJ`로 시작하는 값만**)
3. 좌측 사이드바 **감사 로그** → `/audit-logs`
4. 확인 포인트:
   - [ ] 위에 필터(주체·행위·기간) + [조회] 버튼 보임
   - [ ] 아래 표(시각·주체·행위·대상) 보임 (데이터 없으면 빈 표 — 정상)
   - [ ] [조회] 누르면 필터 반영 / 페이지 이동 동작
   - [ ] 실패 시 우측 빨간 토스트로 메시지

**⚠️ "인증이 필요" / 로그인으로 튕김 (토큰 문제, 코드 아님):**
- ① **`Bearer ` 같이 붙여넣었는지** — 코드가 자동으로 붙이므로 `eyJ...`만. (F12 → Application → Local Storage → `qtai_admin_token` 확인)
- ② **토큰 만료**(보통 30분) → 새로 발급
- ③ F12 → Network → `audit-logs` 요청 Status: **401**=토큰 문제(①②) / **403**=그 회원이 관리자 권한(OPERATOR/REVIEWER/SUPER_ADMIN) 없음
- ※ 카카오 웹 로그인(정식 토큰 발급 경로)은 강태오 담당 — 그 전엔 수동 토큰 필요

---

## PR2~ 개요 (착수 시 세부화)

- [ ] **PR2 · AD-04 신고처리** — 목록+필터(status·targetType)+페이징 + resolve/reject 모달 (C0 재사용)
- [ ] **PR3 · AD-03 AI산출물검증** — 목록+필터 + 승인/반려/숨김 모달. 승인 전 원문 비노출(정책 강태오 확인)
- [ ] **PR4 · AD-08 AI모니터링** — 기간 필터 + 집계 카드(생성잡·검증·QA·체크리스트)
- [ ] **PR5 · AD-05 찬양** — 목록+등록 폼. 수정/숨김은 백엔드 PATCH·hide 선행 필요. 가사·음원·URL 저장 금지
- [ ] **PR6 · D2 권한 가드** — `admin_role` 기반 메뉴 노출/라우트 가드
- [ ] **PR7 · F2 빌드/배포** — `.env` 환경분리 + `npm run build`(dist) + 배포
- [ ] (보류) **AD-01 대시보드 · AD-02 QT관리 · AD-06 공지** — 백엔드 미구현 대기
