# 노트·달력 QA 수정 체크리스트 (2026-06-11) — 브랜치별

> **트랙**: `dev` / **담당**: 김지민(flutter-app)
> **완료 기준(공통)**: 브랜치별 `flutter analyze` 무경고 + 해당 화면 수동 확인 + dev 대상 PR
> **워크플로우**: [workflows/2026-06-11_노트-달력-QA-수정-워크플로우.md](../workflows/2026-06-11_노트-달력-QA-수정-워크플로우.md)
> **승인 플랜**: `C:\Users\G\.claude\plans\todo-cryptic-kazoo.md`

---

## 0. base 브랜치 — ✅ `dev` 확정 (2026-06-11)

- [x] 이번 5개 브랜치 전부 base = `origin/dev`(원격 최신) 기준 (기존 플랜 dev-msa에서 변경)
- [x] `bugfix/note-labels-qt-record` origin/dev(`149d2cf`)에서 생성, upstream 미설정(dev 오발사 방지)
- [x] ⚠️ 확인됨: **dev에 성경 조회 노트버튼 없음**(dev_browser엔 "조회"만). 20260604 분이 dev 미반영 → ② 전 이지윤이 dev에 버튼 추가 선행 필요(라벨·달력·노트탭은 무관)

---

## A. 즉시 착수 (병렬 가능, 타인 의존 없음)

### `bugfix/note-labels-qt-record` — ①⑩ 라벨 ✅ 구현·커밋 완료

- [x] ① `note_models.dart` `'MEDITATION':'묵상'` → `'QT'`
- [x] ① `core/constants/post_category.dart` `'묵상'` → `'QT'`
- [x] ① `catMeditation '묵상'`→`'QT'` (ko/en arb + 생성본 ko/en, 추상 doc) — **영어도 'Meditation'→'QT' 통일**
- [x] ⑩ 새 l10n 키 `navRecord="기록"/"Records"` 추가(arb ko/en + 추상 + 생성본 ko/en, 5곳)
- [x] ⑩ `home_screen.dart` 하단 탭 라벨 → `navRecord`
- [x] ⑩ 노트 목록 제목: `note_list_screen.dart`를 `noteListTitle` → **`navRecord`로 교체** (※ `noteListTitle` 값 자체는 "노트" 유지 — 작성/편집/상세 제목에서 "노트"로 재사용되므로 안 건드림)
- [x] ⑩ `navNote="노트"`는 QT 노트 버튼용으로 유지(변경 X)
- [x] 일반 개념어("나의 묵상"·"묵상 달력"·로그인/온보딩) 미변경 확인
- [x] `flutter analyze` 무경고 (lib/l10n·features/note·features/home·core)
- [x] 커밋: `7405340`(라벨 코드 9파일) + `5e112f7`(QA 문서)
- [ ] push → PR(base dev) — 진행 예정
- 📌 머지 사고 교훈: 로컬 `dev`는 origin/dev와 갈라진 낡은 라인(76앞/61뒤) → **로컬 dev 머지 금지**. 브랜치는 이미 origin/dev 최신 포함(`d465a56`). 잘못된 머지는 `--abort`로 정리함

### `feature/note-calendar-display-qa` — ④⑥⑦ 달력 (전부 `meditation_calendar.dart`)

- [ ] ④ 색 매핑 상수 5종(카테고리별, 날짜탭 체크리스트와 통일) + 점 상한
- [ ] ④ `markerBuilder`로 `CalendarDay.categories`별 색점(eventLoader 점1개 교체)
- [ ] ④ 날짜 탭 체크리스트(기존) 유지 확인
- [ ] ④⑦ `calendarFormat` 상태화 + `onFormatChanged: (f)=>setState`
- [ ] ④⑦ `availableCalendarFormats:{month:'한 달', twoWeeks:'2주'}`
- [ ] ⑥ `daysOfWeekHeight`(24~32) 설정 (+필요시 `rowHeight`)
- [ ] `flutter analyze` 무경고
- [ ] **Galaxy Tab 실기기**: 요일행 잘림·색점·월↔2주 전환 확인
- [ ] 커밋 → push → PR(base dev)

### `feature/note-tab-list-grouping` — ⑤⑧ 노트탭 통합 (`note_list_screen.dart`)

- [ ] 상단 캘린더(월/2주) + 카테고리칩(전체+5종) + 상태필터(전체/임시저장/저장) + 목록 한 화면
- [ ] ⑧ 카테고리 칩 필터 / 상태필터(전체/임시저장/저장) — 탭·섹션 안 만듦
- [ ] 목록↔달력 토글·별도 목록모드 제거(`noteCalendarViewProvider` + AppBar 토글 삭제)
- [ ] 상태필터 provider + 상태필터 쿼리 추가
- [ ] ⑤ 칩 컨텍스트 작성: 기도/회개/감사 칩 → "+ {카테고리} 작성" → N-02 생략, N-03 직행(category 고정)
- [ ] ⑤ FAB 유지(전체 시 N-02). QT·설교 칩은 작성 진입 없음
- [ ] `flutter analyze` 무경고 → 커밋 → push → PR(base dev)

---

## B. 타인 의존 (협의/선행 후)

### `feature/note-sermon-verse-args` — ②⑪ 성경 노트버튼 직행 + 구절 첨부

> 의존: 이지윤(bible `_openSermonNotes` 핸들러). ⑤⑧ 뒤 권장(N-03 진입 패턴 공유).

- [ ] ⚠️ **(이지윤, 선행) dev `bible_browser_screen.dart`에 성경 조회 노트버튼 + `_openSermonNotes(range)` 추가** — dev엔 현재 "조회" 버튼만, 노트버튼 없음(20260604 dev 미반영)
- [ ] (김지민) `NoteEditArgs`에 `verseIds`(+표시용 라벨) 추가
- [ ] (김지민) `NoteEditScreen`이 SERMON+verseIds 수신 시 본문 조회/인용삽입
- [ ] (김지민) 저장 시 `verseIds` 첨부 (repo는 이미 수용 `note_repository.dart:20`)
- [ ] (이지윤) `_openSermonNotes(range)` → `AppRouter.noteEdit`(N-03)로 변경
- [ ] (이지윤) `NoteEditArgs(category:'SERMON', verseIds: range.verses.map((v)=>v.id))`
- [ ] ⑪ N-03 단일 진입 계약 통일(category 고정 [+verseIds] 직행, QT 4섹션 예외) 확인
- [ ] `flutter analyze` 무경고 → 커밋 → push → PR(base dev)

### `feature/note-richtext-integration` — ③⑨ 리치텍스트 소비 (가장 나중)

> 선행 필수: 이지윤 코어(에디터/직렬화/렌더) + 이승욱(note body 저장·`bodyPreview` 평문추출) + Lead(§19.1·body 의미변경).

- [ ] 이지윤한테 계약 3종 수령: ① 직렬화 포맷(body string) ② 에디터 위젯 ③ 렌더 위젯
- [ ] N-03/N-04 `note_format_toolbar` → 리치 에디터 대체
- [ ] create/update 리치 body 전송
- [ ] 노트 상세 렌더(N-04)
- [ ] 나눔 피드/상세 렌더(sharing 영역)
- [ ] ⑨(글자크기·들여쓰기) ③에 포함 해결 확인
- [ ] `flutter analyze` 무경고 → 커밋 → push → PR(base dev)

---

## 막힌 것 / 의존성

| 항목 | 상태 |
| --- | --- |
| dev에 성경 조회 노트버튼 없음 (확정) | ② 전 이지윤이 dev에 버튼+핸들러 추가 선행 (dev-msa엔 있음) |
| ② 이지윤 bible 핸들러 | 멘트 전달 완료, 응답 대기 |
| ③⑨ 이지윤 코어·이승욱 body·Lead §19.1 | 선행 미확정 → 가장 나중 |
| Galaxy Tab 실기기 | ④⑥⑦ 검증에 필요 |
