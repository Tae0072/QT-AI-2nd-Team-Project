# admin-web 코드리뷰 TODO — 진행상황 핸드오프 (2026-06-11)

> 새 세션/계정에서 이어받을 때 **이 문서부터 읽기.** 김지민(admin-web FE)의 코드리뷰 후속 TODO(P1~P5) 작업.

## 1. 한 줄 현황

admin-web 코드리뷰 TODO(P1~P5) **P4·P5b·P5c까지 PR 완료**. **P5a(찬양 수정/숨김)만 남음** — admin-server praise 백엔드 회신 대기.

## 2. 항목별 상태

| 항목 | 내용 | 상태 |
| --- | --- | --- |
| P1 | 로그인 라우팅 (vite proxy `/api/v1/admin/auth`·`/api/v1/auth`→8081) | ✅ 머지(#482) |
| P3a | AD-01 대시보드 실타입 DTO | ✅ 머지(#490) |
| P3b | AD-02 QT관리 풀 CRUD | ✅ 머지(#496) |
| P3c | AD-06 공지 풀 CRUD | ✅ 머지(#501) |
| P2 | 토큰 자동 갱신(refresh, single-flight + 콜백 B) | ✅ 머지(#504) |
| **P5b** | 카카오 키 미설정 안내 Alert + 버튼 비활성화 | ✅ **PR 완료** (`feature/admin-web-kakao-sdk-notice`) |
| **P5c** | 번들 code-split(vite manualChunks) | ✅ **PR 완료** (`chore/admin-web-code-split-redo`) |
| **P4** | 토큰 보관(localStorage) XSS 주석 보강 + HttpOnly 전환 계획 | ✅ **PR 완료** (`docs/admin-web-token-storage-review`, 전환 시점=데모 후 결정 2026-06-11) |
| **P5a** | AD-05 찬양 수정/숨김 행 액션 | ⛔ **백엔드 대기** — admin-server praise `PATCH`/`hide` 미구현. 요청서 전달함(아래) |

## 3. 협의/대기 중인 것

- **P5a 백엔드**: admin-server `domain.praise`에 web 컨트롤러(PATCH·hide) 없음. 요청서 `workflows/2026-06-11_P5a-praise-admin-endpoint-요청.md` 작성·전달 → **강태오(admin-server)/이승욱(praise) 회신 대기**(누가 구현할지 + 응답 DTO·hide가 204인지).
- **P4 HttpOnly 전환**: 전환 시점 = **데모 후(운영 배포 직전)** 결정 완료(2026-06-11). 실제 전환은 운영 전 별도 PR(서버 CORS credentials·쿠키 + FE `tokenStorage` 교체) — 강태오·이승욱 협의 필요.

## 4. 다음 할 일

- (회신 오면) **P5a** — `feature/admin-web-praise-actions`로 hide/update 연결. 요청서 참고.
- **P4 HttpOnly 실전환** — 운영 전, 별도 브랜치. 강태오 최종 확인 후 진행.

## 5. 읽을 문서 (우선순위)

1. **이 문서** (핸드오프)
2. 워크플로우: `doc/workspaces/DevE_김지민/workflows/2026-06-11_코드리뷰-TODO-admin-web-워크플로우.md` (로드맵·항목별 작업순서)
3. 체크리스트: `doc/workspaces/DevE_김지민/todos/2026-06-11_코드리뷰-TODO-admin-web-체크리스트.md` (브랜치별 진행)
4. 코드리뷰 TODO 원본: `doc/workspaces/DevE_김지민/2026-06-10_코드리뷰_TODO_김지민.md`
5. 리포트들: `doc/workspaces/DevE_김지민/reports/2026-06-11_코드리뷰-P*.md` (각 PR 상세)
6. P5a 요청서: `doc/workspaces/DevE_김지민/workflows/2026-06-11_P5a-praise-admin-endpoint-요청.md`
7. study-note: `doc/workspaces/DevE_김지민/study-notes/2026-06-11_토큰-자동갱신-개념정리.md` (access/refresh 개념)
8. 규칙: `CLAUDE.md`(루트), `doc/workspaces/DevE_김지민/00_공통_브랜치_PR_워크플로우_규칙.md`, `skills/SKILL.md`(소크라테스 강/약 모드)

## 6. 작업 규칙·주의 (중요)

- **브랜치 base = `origin/dev`**, push 후 **dev 대상 PR**.
- **브랜치명**: `feature|bugfix|hotfix|chore|release|docs|test`만 허용(CI `pr-validation`). `feat/`·`fix/`는 **막힘**.
- **검증 게이트(admin-web)**: `cd admin-web && npm run typecheck && npm run build`. (⚠️ admin-web은 CI에 typecheck/build 잡이 **없음** → 로컬 검증이 유일한 안전망. 깨진 채 머지될 수 있으니 항상 직접 검증.)
- **dev 머지**: 항상 `git merge origin/dev` (로컬 `dev`는 origin과 갈라진 stale — 절대 머지 금지).
- `pubspec.lock` transitive 다운그레이드 노이즈는 `git checkout -- flutter-app/pubspec.lock`로 폐기(커밋 금지).
- `qtai-server/Untitled`·`flutter-app/devtools_options.yaml`은 stray — 커밋 제외.
- 설계 선택 있으면 🟢 강 모드(선택지 먼저), 기계적이면 🔵 약 모드(요약·이유 설명) — SKILL 규칙.

## 7. dev 로컬 실행 참고

- admin-web: `cd admin-web && npm run dev` (localhost:5173). API는 vite proxy → admin-server(8090)·인증 service-user(8081). **로컬 admin 로그인은 admin-server(8090) 기동 + 관리자 계정 필요**(현재 어려움 — #510 롤백으로 dev-bypass도 제거됨).
- flutter 웹: `flutter run -d chrome --web-port=3000 --dart-define=WEB_DEV_NO_LOGIN=true --dart-define=WEB_DEV_USER_ID=1` (3000 포트 = 백엔드 CORS 허용). 또는 루트 `run-dev-web.sh`.
