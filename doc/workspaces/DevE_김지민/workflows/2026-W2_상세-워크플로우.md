# 김지민 W2 상세 워크플로우 (2026-05-25 ~ 2026-05-29)

> **상위 워크플로우**: [`김지민_W1-W5_워크플로우.md`](../김지민_W1-W5_워크플로우.md) §W2
> **목표**: 자유 노트 핵심 API 4건 완성 (백엔드 우선, Flutter는 디자인 확정 후)
> **완료 기준**: PR 4건 (B1~B4) dev 머지 + 테스트 3종(단위·슬라이스·통합) 통과
> **구현 방식**: 🅐 백엔드 먼저 끝내고 Flutter 몰아치기
> **공휴일**: 2026-05-25(월) — 작업 일정에서 제외 (실작업 4일: 화·수·목·금)

---

## 운영 규칙

### 매일 시작 시 — "오늘 W2 [요일] 시작"
김지민이 이 한 마디만 하면, Claude는 **묻지 않고 자동으로** 다음 순서 수행:
1. `doc/workspaces/DevE_김지민/skills/SKILL.md` 읽기 (학습 규칙 5가지 매일 자동 적용 — 별도 요청 불필요)
2. 이 워크플로우 파일 읽기 + 해당 요일 미체크 항목 확인
3. TodoWrite로 그날치 작업 쪼개기
4. 첫 "선택 필요" 항목에서 SKILL 규칙 1대로 선택지 제시 (이것도 자동)

→ "SKILL 읽어줘"·"선택지 줘" 같은 명령 불필요. **"오늘 W2 [요일] 시작"이 전부.**

### 당겨오기 규칙
- 하루치 체크박스 다 끝나면 → 다음날 첫 항목으로 자동 진행
- 단, **테스트 3종까지 다 끝나야 "당겨오기" 발동** (테스트 없는 진행 금지)
- 빨리 끝낸다고 강제로 당기지 말 것 — 천천히 가도 OK

### 매일 마무리 routine (메모리 등록됨)
1. 이 파일 체크박스 갱신
2. `doc/workspaces/DevE_김지민/todos/진행중-todo.md` 갱신
3. `doc/workspaces/DevE_김지민/reports/2026-MM-DD_작업명-리포트.md` 작성
4. commit + push (PR은 별도 — 작업이 한 단위로 완료될 때만)

### 다음 주(W3) 상세 워크플로우 생성 — "W3 상세 워크플로우 짜줘"
김지민이 이 한 마디만 하면, Claude는 자동으로:
1. `김지민_W1-W5_워크플로우.md` §W3 읽기
2. W2 종료 상태(미완료 항목·당겨오기·부채) 확인 후 W3로 이월
3. 같은 형식의 `2026-W3_상세-워크플로우.md` 생성
4. **공휴일 자동 제외**: 2026-06-03(수)는 W3 일자별 분배에서 제외 (월·화·목·금 4일치만 사용)

---

## 의존성 / 시작 조건

| 의존성 | 상태 | 영향 |
| --- | --- | --- |
| 이승욱 `notes` Entity (#51) | ✅ 머지됨 | B1·B2 시작 가능 |
| 김지민 GET /notes 스켈레톤 (#54) | ✅ 머지됨 | B1 본문 작업 시작 가능 |
| 이승욱 나눔 Entity 4종 (SharingPost/Comment/PostLike/Report) | ⏳ 확인 필요 | 목요일(B3) 아침 확인. 미머지면 B3·B4 순서 조정 |
| 이승욱 Flutter 초기 셋업 PR | ⏳ 확인 필요 | F1~F3 시작 조건 (W2엔 영향 없음 — 디자인 확정 후) |

---

## 일자별 체크리스트

### 월요일 (2026-05-25) — 🛌 공휴일 (작업 없음)

작업 없음. B1은 화요일에 시작.

---

### 화요일 (2026-05-26) — B1: GET /api/v1/notes 본문 + 부채 4·5번 ✅ 완료

> 추정 작업량: 16 항목. 실제 작업량 +1차/2차 리뷰 대응으로 늘어남. PR #92 강태오 수동 머지 대기.

**선택 필요 (Claude가 화요일 시작 시 선택지 제시)**
- [x] Repository 조회 방식 결정 — **② @Query JPQL** 채택 (nullable 동적 필터에 표준 관용구)

**Repository / Service**
- [x] `NoteRepository.java` — `extends JpaRepository<Note, Long>` 선언
- [x] `NoteRepository.java` — 조회 메서드 추가 (@Query JPQL + ESCAPE 절)
- [x] `NoteService.list()` 본문 구현 — Repository 호출 → `Page<Note>` → `NoteListItem` 매핑 → `NoteListResponse`
- [x] `Note` → `NoteListItem` 매핑 위치 결정 — **NoteService 내부 private 메서드** (도메인 경계 보호)

**부채 처리 (PR #54 3차 리뷰 BLOCK)**
- [x] `security/DevUserIdHeaderFilter.java` 신규 — `X-Dev-User-Id` → memberId 주입 (dev 프로파일 한정, 3중 가드)
- [x] `common/exception/GlobalExceptionHandler.java` — `MethodArgumentTypeMismatchException` 핸들러 추가 (잘못된 enum 값 → 400 + 허용 값 목록)

**테스트 3종**
- [x] `NoteServiceTest.java` 신규 — 단위 테스트 (Mock Repository), 11건 (기본 6 + escapeLikeWildcards 5)
- [x] `NoteControllerTest.java` 갱신 — 컨트롤러 직접 호출 + Mockito mock, 3건
- [x] `NoteRepositoryIntegrationTest.java` 신규 — `@DataJpaTest` + H2 (본인 필터·@SQLRestriction·카테고리·페이지네이션·정렬), 6건

**핵심 개념 확인 (SKILL 규칙 4)**
- [x] "Service 레이어가 왜 필요한가" — Controller(HTTP)와 Repository(DB) 사이에서 비즈니스 로직·트랜잭션·도메인 격리를 담당 (SRP)
- [x] "Page<Entity> → Response DTO 매핑은 어디서 하는 게 맞나" — Service 안. api.dto가 internal Entity를 모르도록 보호

**마무리**
- [x] 로컬 테스트 통과 (IDE ▶️ — Gradle 캐시 충돌은 IDE 실행으로 우회)
- [x] commit + push (1차 3 commit + 2차 리뷰 대응 4 commit + 3차 리뷰 대응 1 commit)
- [x] PR 생성 — [PR #92](https://github.com/Tao0072/QT-AI-2nd-Team-Project/pull/92) `feat(note): GET /api/v1/notes 본문 구현 + dev 우회 보강 + 부채 정리`

**추가 처리 (리뷰 대응 — 워크플로우 외 작업)**
- [x] 1차 리뷰: BLOCK 2건 (BaseEntity 영향·테스트 누락) + WARN 3건 처리 (@SQLRestriction·LIKE 이스케이프·Flyway V6 격리)
- [x] 2차 리뷰: WARN 3건 (JPQL deletedAt 중복·escapeLikeWildcards 테스트·빈 문자열 q) + INFO 1건 (getValue null) 처리
- [x] BLOCK BaseEntity.deletedAt — 이승욱 협의 결정 유지, 강태오 수동 머지 대기 (내일 진행 예정)

---

### 수요일 (2026-05-27) — B2: POST /api/v1/notes 자유 노트 생성 ✅ 학습+개선PR 완료

> 추정 작업량: 8 항목 → 실제: B2 학습 구현 + 이지윤 CRUD 겹침 발견 + dev 코드 리딩 + 에러코드 개선 PR

**선택 필요**
- [x] DTO 검증 위치 결정 — **③번 채택** (@Valid 형식 + Service 비즈니스 분리)

**Use Case / Service**
- [x] `CreateNoteUseCase.java` 본문 (SKILL 소크라테스 모드로 학습 구현)
- [x] `NoteService` — `create()` 메서드 + 카테고리별 switch 검증
- [x] `Note.Builder` 호출 — null→"" 변환, confirmSave(), visibility 추가

**Controller**
- [x] `NoteController` — `@PostMapping` + `@Valid @RequestBody NoteCreateRequest`
- [x] 201 Created + ResponseEntity 감싸기

**테스트 3종**
- [x] `NoteServiceTest` — `create()` 단위 테스트 8건 (@Nested CreateTest)
- [x] `NoteControllerTest` — 401 (미인증), 201 (정상)
- [x] 통합 테스트 — 같은 날 같은 카테고리 여러 건 허용 확인

**핵심 개념 확인**
- [x] "201 vs 200 차이" — 200은 일반 성공, 201은 새 리소스 생성
- [x] "@Valid가 실패하면 어떤 예외가 던져지는가" — MethodArgumentNotValidException (Spring 자동)

**이지윤 CRUD 겹침 대응**
- [x] merge 충돌 발견 → merge 취소 (git merge --abort)
- [x] dev 코드 리딩: create 비교 + PATCH 학습 + DELETE 학습
- [x] 내 코드가 더 나은 부분 3가지 발견 (에러코드/응답필드/@Valid)
- [x] 에러코드 개선 PR: `bugfix/note-category-error-codes` push 완료
- [ ] PR 생성 — GitHub 500 에러로 대기 중

**마무리**
- [x] 리포트 작성 + todo 갱신 + 워크플로우 갱신
- [x] commit + push 완료

---

### 목요일 (2026-05-28) — B3: GET /api/v1/sharing-posts ✅ 구현+테스트 완료, PR 대기

> ✅ **시작 전 확인 결과**: `SharingPost` Entity는 dev에 머지돼 있었음. 단, 07 §F-10 명시 컬럼 `nickname_snapshot`이 누락돼 있어 이승욱 협의 후 V13으로 추가.
> 오전엔 어제 PR(에러코드/DTO) dev 머지(#123) + 봇 BLOCK 대응으로 04 API 명세 에러코드 반영(별도 리포트).

**선택 필요**
- [x] 응답 DTO 설계 — **① 목록·상세 DTO 분리** 채택 (목록 bodyPreview, 상세는 B4. 노트 때와 동일 논리)
- [x] B3 범위 — **A(컬럼 추가)** 채택, 이승욱 협의 완료
- [x] likedByMe 계산 — **배치 조회(IN)** 채택 (N+1 방지)
- [x] 필터 범위 — 정렬(최신/좋아요/댓글) + category + q **전부 포함** (재방문 방지)

**Entity/DB (계획 외 추가)**
- [x] `V13__add_nickname_snapshot_to_sharing_posts.sql` — nickname_snapshot(20), snapshot_verse_label(100) nullable ADD
- [x] `SharingPost` 엔티티 필드 2개 추가 (ddl-auto: validate 정합)

**Repository / Service**
- [x] `SharingPostRepository.java` — `search()` PUBLISHED + category + q(LIKE 이스케이프) 조회
- [x] `PostLikeRepository.java` — `findLikedPostIds()` likedByMe 배치 조회
- [x] `SharingPostService.java` — 필터·정렬 화이트리스트·N+1 방지·매핑 (※ SharingService 아닌 신규 SharingPostService)

**Controller**
- [x] `SharingPostController.java` 신규 — `@GetMapping` `/api/v1/sharing-posts`
- [x] N+1 방지 확인 — likedByMe 배치 조회로 항상 쿼리 2번

**테스트 3종**
- [x] 단위(SharingPostServiceTest 6) / 컨트롤러(SharingPostControllerTest 2) / 통합(SharingPostRepositoryIntegrationTest 3) — 11건 통과

**핵심 개념 확인**
- [x] "N+1 문제가 뭔가" — 목록 1번 + 항목마다 N번. 반복문 안 조회가 원인. IN 배치로 2번에 끝냄
- [x] "Pageable sort + 화이트리스트" — sort는 URL 문자열이라 임의 필드 차단 위해 허용 목록만 통과

**마무리**
- [x] 테스트 통과 + 리포트/todo/워크플로우 갱신
- [ ] commit + push + PR (PR 본문 보강 후 — 금요일)
- [ ] 공지 3건 발송 (V13 DB / 이승욱 발행로직 / 이지윤 신규파일)

---

### 목요일에 당겨 완료 — B4: GET /api/v1/sharing-posts/{postId} ✅

> B3 직후 같은 브랜치(`feature/sharing-posts`)에서 이어 구현. "한 브랜치 = 나눔 조회 기능".

**선택 필요**
- [x] 404 처리 — **`findByIdAndStatus(PUBLISHED)` + orElseThrow** 채택 (HIDDEN/DELETED/없음 모두 자동 404)
- [x] verses[] 처리 — **빈 배열(v2 이관)**. 절 본문 스냅샷 저장 수단 없음 + 다중 절은 v2(07 §19.2, 이지윤·이승욱 영역)

**Service / Controller**
- [x] `SharingPostService.getDetail(memberId, postId)` — PUBLISHED만, 404, likedByMe·ownedByMe 계산
- [x] `GetSharingPostUseCase` 신규 포트
- [x] Controller `@GetMapping("/{postId}")`
- [x] 본인 글 여부(`ownedByMe`) 응답 필드 포함

**상세 DTO**
- [x] `SharingPostResponse`(스켈레톤→채움) + `VerseSnapshotDetail` + `VerseLine` (§4.4.2 중첩)

**테스트 3종**
- [x] 단위(서비스 +3) / 컨트롤러(+2) / 통합(+1) — 나눔 조회 전체 17건 통과

**핵심 개념 확인**
- [x] "쿼리 메서드(findByIdAndStatus) — 이름으로 SQL 자동 생성"
- [x] "조회 vs 필터 — WHERE에 status 조건 붙은 조회"
- [x] "DTO 중첩 — record 필드에 record를 적으면 JSON 중첩"

**마무리**
- [x] 테스트 통과 + 리포트(B3+B4)/todo/워크플로우 갱신
- [ ] commit + push + PR (금요일)

---

### 금요일 (2026-05-29) — PR·공지 마무리 + W3 시작

**W2 마무리**
- [x] 나눔 조회 PR(B3+B4) — **목(5/28) 저녁 이미 생성됨**(GitHub 봇 슬랙 알림 17:38 KST). 열린 PR 상태(dev 미머지). 오늘 문서 push로 자동 갱신
- [x] 공지 3건 발송 (V13 DB / 이승욱 발행로직 / 이지윤 신규파일) — 슬랙 전달 완료
- [x] W2 종료 자기 점검 (아래 섹션)
- [x] 주간 회고 — `reports/2026-W2_주간회고.md` 작성 완료

**W3 시작 (브랜치 분리 — 한 브랜치 = 한 기능)** — 오늘 범위 밖(별도 턴)
- [ ] note 브랜치: 노트 미완(노트→나눔 공유 §4.3.8, JournalEvent 등)
- [ ] sharing 브랜치: 나눔 쓰기(publish / like / comment)
- [ ] (보류) 토큰 공유(Share) 갈래 — 담당·우선순위 협의

---

## Flutter 섹션 (디자인 확정 후 진행 — W2 일정 외)

> 디자인이 확정되면 W3 또는 W4 초반에 시작.

- [ ] **F1. N-01** — 노트 목록 (5개 카테고리 탭 + `GET /notes` 연동)
- [ ] **F2. N-02** — 개인 노트 카테고리 선택 (기도/회개/감사)
- [ ] **F3. N-03** — 개인 노트 작성 초안 (`category=PRAYER/REPENTANCE/GRATITUDE`, 제목+본문, `qt_passage_id=null`)

---

## W2 종료 시 자기 점검 (금요일 마무리)

- [~] 백엔드 PR 4건 — B1 #94·B2 #123 **머지** / B3+B4 **열린 PR(dev 미머지, 머지는 강태오)**
- [x] 각 PR에 테스트 3종(단위·슬라이스·통합) 포함 — 나눔 조회 17건(단위 9·컨트롤러 4·통합 4) 확인
- [x] `application-dev.yml`에 dev 우회 필터 활성화 확인 — `qtai.security.dev-bypass: true` → `DevSecurityConfig`+`DevUserIdHeaderFilter`(X-Dev-User-Id)
- [ ] (생략) Postman/curl 수동 테스트 — 로컬 서버 기동 필요 + gh 미인증, 테스트 17건으로 갈음
- [ ] (W3 이월) 나눔 공개 API 시작 조건 점검 — 이승욱 발행로직 의존성

---

## 진행률 (자동 계산)

> 매일 마무리 시 갱신. `완료 / 총 체크박스` 형식. (선택 필요·핵심 개념·마무리 포함)

- 월(공휴일): — / —
- 화(B1): **16 / 16 ✅** (+ 리뷰 대응 8건 보너스)
- 수(B2): **16 / 16 ✅** (PR #123 머지 완료)
- 목(B3 목록 + B4 상세): **34 / 34 ✅** (B4까지 당겨 완료) + 04 API 반영 + V13 협의 보너스 + PR 생성(17:38)
- 금: PR(기생성 확인)·공지(슬랙 완료)·자기점검·주간회고 **마무리 ✅** / W3 착수는 별도 턴
- **W2 백엔드 B1~B4**: **66 / 66 (100%) ✅** (PR·공지·회고까지 마감)

---

## 참고 문서

- 요구사항: `doc/standards/07_요구사항_정의서.md` §F-16 (자유 노트)
- API 계약: `doc/standards/04_API_명세서.md` §4.3 (노트 API), §4.4 (나눔 API)
- ERD: `doc/standards/02_ERD_문서.md` §notes, §sharing_posts
- 도메인 패키지 규칙: `CLAUDE.md` §4
- 코드 컨벤션: `CODE_CONVENTION.md`
- 학습 규칙: `doc/workspaces/DevE_김지민/skills/SKILL.md` (매일 자동 로드)
