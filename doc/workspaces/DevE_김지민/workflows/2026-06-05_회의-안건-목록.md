# 2026-06-05 · 회의 안건 목록 (김지민)

> 출처: 2026-06-05 W3 Day2 작업 중 발견·결정 보류된 항목 모음
> 상태: 회의 상의 대기

---

## 1. `shares`(토큰 링크 공유) 죽은 코드 정리
- **배경**: sharing 도메인에 `shares`(토큰 링크 + 스냅샷) 스텁 11개 + report의 `GetSharedSnapshotUseCaseMock` 1개가 있음. **04 명세에 없고, 코드 소비처 0**(report 실제 코드는 `GetSharingPostUseCase` 사용). 공개 웹페이지도 없어 토큰 링크를 띄울 화면 자체가 없음.
- **결정 필요**: v1에서 완전 제외(삭제)할지 / v2 대비 보존할지.
- **참고**: 상세 분석·삭제 플랜 → `workflows/2026-06-05_shares-죽은코드-정리-플랜.md`
- ⚠️ 외부 공유(share_plus, §19.1)와는 **다른 것** — 삭제해도 외부공유 영향 없음.

## 2. 묵상(QT) 노트 작성·수정 화면 부재
- **배경**: 묵상 노트 CRUD는 **백엔드 100% 완비**(PATCH가 4섹션 처리). 그런데 4섹션 **작성·수정 Flutter 화면(Q-06)이 없음**. `today_qt_screen`의 "노트" 버튼은 `onPressed: () {}` 빈 껍데기.
- **영향**: 오늘 N-04에서 묵상 노트는 [수정] 버튼을 숨김(편집할 화면이 없어서).
- **결정 필요**: Q-06(묵상 노트 작성·수정 화면) **담당 확정**(노트=김지민 / QT화면=bible 영역 경계 애매) + 일정.

## 3. 노트 본문 리치텍스트(색/크기/하이라이트)
- **배경**: 오늘 서식 툴바를 **마크다운 마커 방식(A)**으로 구현(굵게/기울임/제목/목록/인용/체크박스/구분선). 백엔드 `body`가 평문이라 마크다운으로 충분.
- **한계**: **색상·글자크기·형광펜은 마크다운으로 불가** → 진짜 리치텍스트가 필요하면 `body` 저장포맷을 Delta/HTML로 바꿔야 함.
- **결정 필요**: 리치텍스트를 v1.x에 도입할지. 도입 시 02 ERD `notes.body`·04 API 계약 변경 + 이지윤 툴바(B-03)와 저장포맷 합의 필요(혼자 결정 불가).

## 4. 설교노트 편집 화면(B-03)
- **배경**: N-04 단일본문 편집(N-03 재사용)은 `verseIds`를 안 보냄 → 설교노트를 PATCH하면 **인용 절(note_verses)이 삭제됨**(04 §4.3.6). 그래서 오늘 N-04 [수정]을 **자유노트(기도/회개/감사)에만** 노출하고 설교노트는 제외.
- **결정 필요**: 설교노트 편집은 **절 선택 UI가 있는 화면(B-03, 이지윤)**에서 처리. 담당·연결 시점 합의.

## 5. pubspec 공용 패키지 추가 (오늘 추가함)
- **추가**: `table_calendar ^3.2.0`(묵상 달력), `share_plus ^12.0.2`·`path_provider`(외부 공유). 의존성으로 `intl`·`simple_gesture_detector` 등 포함.
- **공유 사항**: `pubspec.yaml`/`pubspec.lock` 변경 → 팀원은 pull 후 `flutter pub get` 필요.

## 6. 04 SSoT 문서저장소 반영 (Lead 작업 — 김지민은 전달만)
- 이전에 추가한 `PATCH /sharing-posts/{id}/show`(나눔 되돌리기) 등이 **로컬 캐시(doc/standards) 사본만 갱신**되고 **원본 문서 저장소(master)** 엔 미반영.
- **김지민 할 일**: "원본에도 반영해 주세요"라고 **Lead에게 전달만**. 직접 master 문서 수정 불필요(계약 문서 변경은 Lead 검토 — CLAUDE.md §2).
