# 관리자웹 조치 TODO 워크플로우

근거: 관리자웹 QA/비즈니스 검수 리포트(2026-06-11), 대상 AD-01~AD-10 · 작성 2026-06-15

아래는 검수에서 "아직 안 되거나 / 수정 / 구현이 필요"로 판정된 항목만 추린 실행 목록이다.

> **진행 상황(2026-06-16 갱신):** P1 2건(AD-03·AD-05)과 P3 콘솔 경고는 **이미 dev에 반영**되어 있었음(2026-06-12 머지분). P2 AD-01 CTA·AD-08 가독성은 **PR #647**(`feature/admin-web-dashboard-cta-monitoring`)로 완료. **AD-04 신고 처리**, **AD-06 공지 발행 알림**, **AD-07 감사 로그 회귀**는 샘플 데이터로 확인 완료(신고 #1 처리, #2 반려, 공지 #1 발행, 알림 2건 생성, 감사 필터 확인). **AD-10 배치 로그 회귀**는 현재 샘플 0건이라 잔여. **관리자 해설 생성 트리거**는 2026-06-15 반영 확인 완료(#649). **시뮬레이터 전용 관리 화면(조회+숨김)**도 **PR #652**(AD-14)로 완료. **시뮬레이터 승인→게시 자동 배선**은 해설/용어 승인 흐름과 대칭으로 `AiAssetReviewService`에 연결 완료. **앱 재생용 QT 영상 관리(AD-20)**는 **PR #686**으로 올려 3차에 걸친 리뷰 대응까지 완료했다: dev 머지(충돌 5건 해결), 원본 수정 400(`bibleBookId` 전송) 해결·구간 예시 저장 방지, 원본/클립 **soft-delete + cascade**(프로젝트 공통 정책 통일), 삭제 클립 앱 누수 차단(service-bible 유저조회에 `deleted_at` 필터), 감사 로그를 **서비스 트랜잭션 내부로 이동**(변경과 원자적)·before-state·JSON 직렬화, 컨트롤러 11개 엔드포인트 MockMvc + 리포지토리 `@DataJpaTest`, 04 명세·openapi 동기화, 그리고 **직접 클립 생성(시작/끝 초 지정) 하이브리드** 추가. 절↔초 자동 매핑은 후속 구현 문서로 기록. 로컬에서 CI 게이트(빌드·테스트·커버리지·requirements-guard) 전부 통과 재현. 상태 칸: ✅완료 · 🔄진행중 · ☐미착수.

---

## P1 · 서비스 신뢰성에 직접 영향 (먼저 처리)


| 상태  | 항목 · 해야 할 일                                                                                                                                                                           | 영역    | 완료 기준               |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ------------------- |
| ✅   | **AD-03 AI 산출물 검증 — 대시보드 지표와 목록 불일치 해결** ~~대시보드 "AI 검증 대기 857"과 AD-03 목록 "데이터 없음"이 어긋남.~~ **이미 dev 반영 완료**(2026-06-12 count-쿼리 수정 + 기본필터 VALIDATING). 검증: 목록 857건 정상 반환, 대시보드와 일치 확인. | BE+FE | ✅ 일치·승인/반려 동작 확인    |
| ✅   | **AD-05 찬양 큐레이션 — 404성 오류 문구 제거** ~~"요청한 리소스를 찾을 수 없습니다." 노출.~~ **이미 dev 반영 완료**. 검증: `GET /api/v1/admin/praise-songs` → 200 + 빈 목록(totalElements:0), 404 없음.                         | BE+FE | ✅ 정상 공백 표시, 404 미노출 |


---

## P2 · 운영 편의·정확성 보완


| 상태  | 항목 · 해야 할 일                                                                                                                                                                              | 영역     | 완료 기준                         |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ----------------------------- |
| ✅   | **AD-01 대시보드 — 문제 상태에 "다음 행동(CTA)" 추가** **PR #647 완료**. 문제 상태(검증대기·신고·시뮬레이터·해설)에만 조건부 CTA + 정밀 딥링크(`/ai-assets`, `/reports?status=`, `/qt-passages?focusId=`) + 카운트 3칸 높이 정렬. 라이브 확인 완료. | FE     | ✅ 1클릭 조치 화면 진입                |
| ✅   | **AD-08 AI 운영 모니터링 — 실패 사유 가독성·노출 범위 정리** **PR #647 완료**. 실패/차단 사유 count 내림차순 상위 5개 + "그 외 N종" 요약, 배치 오류 ellipsis+Tooltip. "원문 미포함" 정책 유지.                                               | FE+BE  | ✅ 요약 집계·원문 미노출                |
| ✅   | **AD-04 신고 처리 — 샘플 데이터로 처리/반려 + 감사기록 검증** `POST /api/v1/admin/reports/test-seed`로 테스트 신고 2건 생성 후 신고 #1은 `RESOLVED`, 신고 #2는 `REJECTED`로 처리했다. 감사 로그 `REPORT_RESOLVE` #62, `REPORT_REJECT` #63 생성 및 `actionType` 필터 노출을 확인했다.                                                                                 | 데이터·QA | ✅ 처리·반려 결과와 감사 로그 연동 확인         |
| ✅   | **AD-06 시스템 공지 — 발행 시 알림 생성 검증** 샘플 공지 #1 발행 확인. `NOTICE_PUBLISH` 감사 로그 #5의 `notificationResult`가 `requestedCount=2`, `createdCount=2`, `failedCount=0`으로 기록되어 발행 시 사용자 알림 생성 성공을 확인했다.                                                                                                            | 데이터·QA | ✅ 발행 시 사용자 알림 생성 확인             |
| ✅   | **AD-07 감사 로그 — 샘플 회귀 검증** 감사 로그 목록 총 61건 확인. `NOTICE_CREATE`, `NOTICE_PUBLISH`, QT 영상 액션 등 관리자 액션 샘플이 노출되고, `actionType=NOTICE_PUBLISH` 필터가 정상 동작함을 확인했다.                              | 데이터·QA | ✅ 샘플 기반 목록/필터 노출 및 추적 가능성 확인 |
| ☐   | **AD-10 배치 로그 — 샘플 회귀 검증** `/api/v1/admin/ai/batch-run-logs` 조회 결과 `totalElements=0`으로 아직 샘플 없음. 배치 성공/실패 샘플 1건 이상 생성 후 목록·필터·실패시각·오류유형·재시도 판단 가능성 재검수 필요.                              | 데이터·QA | 배치 성공/실패 샘플 기반 추적 가능성 확정 |


---

## P3 · 정리 (기능 차단 아님)


| 상태  | 항목 · 해야 할 일                                                                                                                        | 영역  | 완료 기준      |
| --- | ---------------------------------------------------------------------------------------------------------------------------------- | --- | ---------- |
| ✅   | **콘솔 경고 정리 (Ant Design)** **이미 정리됨**. `destroyOnClose` 0건(→`destroyOnHidden` 일괄 교체, 2026-06-12 머지), `Spin tip` 유발 `tip=` 사용 0건 확인. | FE  | ✅ 대상 경고 없음 |


---

## 권장 진행 순서 (워크플로우)

① P1 데이터 연결 2건(AD-03 지표↔목록, AD-05 찬양 404) 먼저 해결
→ ② P2 대시보드 CTA·모니터링 가독성 정리 후 신고/공지/감사·배치 샘플 데이터 회귀 검증
→ ③ P3 콘솔 경고 정리

**각 항목 처리 절차:** dev에서 작업 브랜치 생성 → 수정/구현 → 검증(`typecheck·build·test`, 서버는 `gradle build·test`) → PR(dev 대상) → CI 통과 → 머지. 데이터·QA 항목은 샘플 생성 스크립트를 남겨 재현 가능하게 한다.

> ※ 분류 근거: 검수 판정에서 "적합"으로 끝난 화면(AD-02 오늘 QT 관리, AD-09 체크리스트)은 조치 항목에서 제외했고, "적합(데이터 필요)/보완 필요/부적합/보류"에서 나온 액션만 모았다.

---

## 진행 중 / 후속 (검수 TODO 외 — CTA 작업에서 파생)


| 상태    | 항목                                                                                                                                                                                                                                                                                                                                 | 브랜치                                          | 메모                                                                                                                                                           |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ✅ 완료  | **관리자 해설 생성 트리거** — 미생성 해설을 관리자가 직접 생성 요청(특정 본문 지정). 기존 `seedToday` 로직을 본문 단위(`seedForPassage`)로 일반화해 `POST /api/v1/admin/ai/qt-passages/{qtPassageId}/explanations/generate`로 노출 + 감사로그(`AI_EXPLANATION_GENERATE_REQUEST`) + 대시보드 FE 버튼 연결. AD-01 해설 CTA가 "검증하러 가기"(임시)였던 이유 해소.                                                  | 반영 완료                                        | 확인: `AdminAiExplanationController`, `AiService.generateQtPassageExplanation`, `DashboardPage` 버튼, 관련 컨트롤러/서비스 테스트 존재                                         |
| ✅ 완료  | **시뮬레이터(QT 영상) 전용 관리 화면(조회+숨김)** — 클립 목록·상태필터·숨김(AD-14). **PR #652** 머지. 대시보드 시뮬레이터 CTA를 전용 화면(`/simulator-clips?qtPassageId=`)으로 교체 완료. 기능격차 B-1 해소.                                                                                                                                                                              | `feature/admin-web-simulator-management`(머지) | 확인: `AdminSimulatorClipController`(조회+숨김), `SimulatorClipsPage`, hide 감사로그 `SIMULATOR_CLIP_HIDE`                                                             |
| ✅ 완료 | **시뮬레이터 승인→게시 자동 배선** — AI 산출물 `SIMULATOR + QT_PASSAGE` 승인(`activateForTarget=true`) 시 `simulator_clips` APPROVED 클립 생성, HIDE 시 공개 클립 숨김. 해설/용어 승인 흐름과 대칭으로 `AiAssetReviewService`에서 study UseCase 호출을 연결. | 현재 작업 | 확인: `AiAssetReviewService` SIMULATOR 분기, `StudyPublishClient`, `AiAssetReviewServiceTest` 승인/비활성/HIDE 케이스 |
| ✅ 완료 | **QT 영상 관리(앱 재생용, AD-20) — PR #686** — 원본 영상 URL·duration 등록/수정/조회, 절별 영상 구간 등록/수정, QT 본문별 `qt_video_clips` 생성/조회, 상태 변경, 대시보드 CTA `/qt-videos?qtPassageId=` 연결. 디버깅: 원본 수정 400(`bibleBookId` 전송) 해결, 빈 구간 모달 기본 2건 예시 저장 방지, 클립 목록 라벨 분리(클립 ID/QT 본문 ID/원본 영상 ID). **리뷰 대응(3회차):** ① 원본/클립 삭제를 **soft-delete + cascade**로 통일(`deleted_at`·`active_unique_key` 해제, 목록 `deleted_at IS NULL` 필터) ② 삭제 클립이 앱에 노출되던 누수를 **service-bible 유저조회 쿼리에 `deleted_at` 필터** 추가로 차단 ③ **감사 로그를 서비스 `@Transactional` 내부로 이동**(변경과 원자적, MusicTrackService 패턴) + before-state·Jackson 직렬화 ④ 컨트롤러 11개 엔드포인트 MockMvc + 리포지토리 `@DataJpaTest`(admin·bible) ⑤ 04 명세·openapi 동기화, qtvideo 도메인 근거 문서 ⑥ **직접 클립 생성(시작/끝 초 지정) 하이브리드**(`POST /clips/manual`) 추가, 절↔초 자동 매핑은 후속 문서화. 로컬 검증: 원본 #4 구간 21개, 본문 #2 클립 #4 APPROVED, 실제 mp4(GitHub release)로 `/api/v1/qt/2/video` **READY** 반환. CI 게이트(빌드·테스트·커버리지·requirements-guard) 로컬 전부 통과. | `feature/admin-qt-video-management`(**PR #686**) | 확인: `AdminQtVideoController/Service`, `QtVideoClipRepository`(admin·bible), `QtVideoService`(bible), `QtVideosPage`, `qtVideos.ts`, `AdminQtVideo*Test`, `QtVideoRepositoriesDataJpaTest`, `QtVideoClipRepositoryTest` |
| 🔄 부분완료 | **AD-06 공지 발행 알림 / AD-07 감사 로그 회귀 / AD-10 배치 로그 회귀** — AD-06은 공지 #1 발행 및 알림 2건 생성 성공, AD-07은 감사 로그 목록/필터 샘플 확인 완료. AD-10은 배치 실행 로그 샘플이 0건이라 잔여.                                                                                                                                                                                                                                                      | —                                            | AD-10 배치 성공/실패 샘플 생성 후 재검수 필요                                                                                                                                                        |


### 완료 PR

- **PR #647** `feat(admin): 대시보드 문제상태 CTA + AI 모니터링 가독성 개선 (F-06/F-14)` — AD-01 CTA + AD-08 가독성 + 카드 정렬.
- **PR #649** `관리자 해설 생성 트리거` — 특정 QT 본문 해설 생성 job 시딩 API, 감사로그, 대시보드 버튼 연결. 2026-06-15 반영 확인.
- **PR #650** `감사 조회 뷰 확장` — `targetType` nullable 필터 + allowlist(`AI_EXPLANATION_GENERATE_REQUEST`/`SIMULATOR_CLIP_HIDE`).
- **PR #652** `시뮬레이터 전용 관리 화면(조회+숨김, AD-14)` — `findForAdmin` + 목록/숨김 컨트롤러 + 대시보드 CTA 교체.
- **PR #653** `해설 트리거 워크플로우 권한표기 정정` — REVIEWER/OPERATOR → REVIEWER/SUPER_ADMIN.
- **현재 작업** `SIMULATOR` 승인→게시 자동 배선 — `AiAssetReviewService`에서 study 게시/숨김 UseCase 호출 연결.
- **PR #686** `QT 영상 관리(AD-20)` — 앱 재생용 원본 영상·절 구간·QT 클립 운영 API/화면. 3회차 리뷰 대응 완료: soft-delete+cascade 통일, 삭제 클립 앱 누수 차단(service-bible `deleted_at` 필터), 감사 로그 서비스 트랜잭션 이동+before-state+직렬화, 컨트롤러 11개 MockMvc+`@DataJpaTest`, 04/openapi 동기화, 직접 클립 생성(시작/끝 초) 하이브리드. 절↔초 자동 매핑은 후속 문서(`2026-06-16_qtvideo-auto-verse-timecode-후속.md`). 누적 커밋 `e4121f07`→`20b8b1f5`. 로컬 CI 게이트 전부 통과 재현, 실제 mp4로 앱 API READY 확인.
- (P1 AD-03/AD-05·P3 콘솔경고는 2026-06-12 머지분에서 이미 해결돼 별도 PR 불필요.)
