# 김지민 W3 상세 워크플로우 (2026-06-01 ~ 2026-06-05)

> **상위 워크플로우**: [`김지민_W1-W5_워크플로우.md`](../김지민_W1-W5_워크플로우.md) §W3
> **목표**: 나눔 쓰기(공개·좋아요·댓글·삭제) 백엔드 API 완성 (백엔드 우선, Flutter는 디자인 확정 후)
> **완료 기준**: 기능별 커밋 + 테스트 3종(단위·슬라이스·통합) 통과 + dev-console로 동작 눈으로 확인
> **구현 방식**: 🅐 백엔드 먼저 끝내고 Flutter 몰아치기
> **공휴일**: 2026-06-03(수) — 작업 일정에서 제외 (실작업 4일: 월·화·목·금)

> ⚠️ **2026-05-29 정정 (최신 dev 기준)** — 본 문서 작성 후 dev 상태가 바뀌어 아래 가정 일부가 stale함. 정정 사항:
>
> - **신고(`POST /reports`)는 W3 대상 아님** — dev에 이미 완료(#140), reports 테이블도 V17로 존재. ❌ "금요일 신고 + V14 신규" 무효. → 금요일은 **댓글 마무리 + W3 마감**으로 대체.
> - **브랜치 전략 = 한 브랜치 `feature/sharing-write`** (per-feature 4개 분리 아님). 4기능을 기능별 **커밋**으로 쌓고 PR 1개. (2026-05-29 사용자 결정)
> - **남은 W3 = 공개·좋아요·댓글·공유글삭제 4개.** 정확한 현황은 → [`../김지민_작업_진행현황.md`](../김지민_작업_진행현황.md) 참조.

---

## 운영 규칙

### 매일 시작 시 — "오늘 W3 [요일] 시작"

김지민이 이 한 마디만 하면, Claude는 **묻지 않고 자동으로** 다음 순서 수행:

1. `doc/workspaces/DevE_김지민/skills/SKILL.md` 읽기 (학습 규칙 5가지 매일 자동 적용)
2. 이 워크플로우 파일 읽기 + 해당 요일 미체크 항목 확인
3. TodoWrite로 그날치 작업 쪼개기
4. 첫 "선택 필요" 항목에서 SKILL 규칙 1대로 선택지 제시 (자동)

### 당겨오기 규칙

- 하루치 체크박스 다 끝나면 → 다음날 첫 항목으로 자동 진행
- 단, **테스트 3종 + dev-console 확인까지 다 끝나야 "당겨오기" 발동**
- 빨리 끝낸다고 강제로 당기지 말 것 — 천천히 가도 OK

### 매일 검증 루틴 (테스트 3종 통과 후 — W3 신규)

> 백엔드만 짜면 동작이 안 보이므로 **눈으로 확인**하는 단계를 매일 넣는다.

1. 앱 dev 프로파일 실행 (`spring.profiles.active=dev`)
2. 브라우저에서 `http://localhost:8080/dev-console.html` → `X-Dev-User-Id` 입력 → 그날 기능 버튼 클릭 → 응답 status·JSON 확인
3. `http://localhost:8080/h2-console` 접속 → 해당 테이블 행(스냅샷·likeCount·comment·report) 확인
   - JDBC: `jdbc:h2:mem:qtai;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MYSQL`, user `sa`, pw 빈값
   - ⚠️ 인메모리라 앱 재시작 시 데이터 초기화. 흰 화면이면 `DevSecurityConfig`에 `.headers(h -> h.frameOptions(f -> f.sameOrigin()))` 1줄 추가

- **자동 테스트(정확성) + 콘솔(눈으로 동작 체감)** 이중 확인.

### 매일 마무리 routine (메모리 등록됨)

1. 이 파일 체크박스 갱신
2. `todos/진행중-todo.md` 갱신
3. `reports/2026-MM-DD_작업명-리포트.md` 작성
4. commit + push (PR은 별도 — 기능이 한 단위로 완료될 때만)

### 다음 주(W4) 상세 워크플로우 생성 — "W4 상세 워크플로우 짜줘"

1. `김지민_W1-W5_워크플로우.md` §W4 읽기
2. W3 종료 상태(미완료·당겨오기·부채) 확인 후 W4로 이월
3. 같은 형식의 `2026-W4_상세-워크플로우.md` 생성

---

## 의존성 / 시작 조건

| 의존성                                                      | 상태                                              | 영향                                                              |
| ----------------------------------------------------------- | ------------------------------------------------- | ----------------------------------------------------------------- |
| 나눔 Entity (`SharingPost`·`Comment`·`PostLike`)            | ✅ 머지됨 (V5+V13, 엔티티 완성)                   | 공개·좋아요·댓글·삭제 시작 가능                                   |
| `PostLikeRepository`                                        | ✅ 완성 (findLikedPostIds)                        | 좋아요 바로 시작                                                  |
| `member.api.GetMemberUseCase` (+ `MemberResponse.nickname`) | ✅ 실재                                           | 공개 시 닉네임 박제 / 댓글 닉네임                                 |
| **`reports` 테이블**                                        | ⏳ **마이그레이션 없음 → V14 신규 (이승욱 협의)** | 신고(금) 착수 전 협의 필수. V13 전례                              |
| `CommentRepository`                                         | ⏳ 없음 → 신규 작성                               | 댓글(목) 작업 시 생성                                             |
| B3+B4 PR (나눔 조회) dev 머지                               | ⏳ 강태오 대기                                    | W3 시작 시 머지 상태 확인. 미머지여도 같은 도메인이라 작업은 가능 |
| Flutter 초기 셋업 + 디자인 확정                             | ⏳ 확인 필요                                      | S-01~03 Flutter는 W3 일정 외 (디자인 확정 후)                     |

---

## W3 착수 전 정리 (월 오전, 짧게)

- [x] **W2 마감 문서 7개 commit + push** (직전 보류분: 주간회고·갱신 워크플로우·파일레퍼런스 정정 등) → 열린 PR(`feature/sharing-posts`) 자동 갱신
- [x] **dev 최신 pull** + B3+B4 PR 머지 상태 확인 (GitHub 웹 / 슬랙 봇)
- [x] **`dev-console.html` 뼈대 생성** — `qtai-server/src/main/resources/static/dev-console.html`. `X-Dev-User-Id` 입력란 + baseURL + "나눔 공개" 섹션 + 재사용 `call()/show()`. (2026-06-01)
- [x] **H2 콘솔 흰화면 방지** — `DevSecurityConfig`에 frameOptions sameOrigin 1줄 추가 완료. (2026-06-01)

---

## 일자별 체크리스트

### 월요일 (2026-06-01) — 나눔 공개 `POST /api/v1/notes/{id}/share` (F-10, §4.3.8)

> 브랜치: `feature/sharing-publish`. W3에서 가장 foundational — 피드에 실제 데이터를 만드는 흐름. 닉네임·본문 스냅샷 박제 + 원자성이 핵심.

> 진행: 브랜치는 `feature/sharing-write`(한 묶음). 2026-05-29 ④까지 완료, ⑤+테스트 이월.

**선택 필요 (완료)**

- [x] 엔드포인트 배선 위치 — **① NoteController가 sharing.`PublishNoteUseCase` 호출(cross-domain)** 채택 (ArchUnit web→api 허용, 순환 미검사 확인)
- [x] 스냅샷 박제 원자성 — `@Transactional` 한 트랜잭션 채택
- [x] 중복 공개 차단 — **사전 조회(existsByNoteId)→409** + DB UNIQUE backstop / snapshotBody — body 우선, 비면 4섹션 합성

**Use Case / DTO**

- [x] `PublishNoteRequest` — `@NotNull Boolean confirmNicknamePublic` + `Boolean commentsEnabled`
- [x] `PublishNoteUseCase.publish(memberId, noteId, request)` 시그니처
- [x] `SharingPostService.publish()` — 검증→노트조회→닉네임박제→스냅샷 저장 + `SharingPost.publish()` 정적 팩토리 + `existsByNoteId` + `ErrorCode.SHARING_POST_ALREADY_SHARED`

**닉네임 박제 / 의존**

- [x] `GetMemberUseCase.getMember(memberId).nickname()` → `nicknameSnapshot` 박제 (실제 member 포트 직접 주입)
- [x] noteId 중복 공유 방지 (`existsByNoteId` + `SharingPost.noteId` UNIQUE)
- [ ] (권장) `05_시퀀스_다이어그램.md` 공개 스냅샷 원자성 흐름 fetch — 생략(설계 확정됨)

**Controller** (2026-06-01 완료)

- [x] `NoteController @PostMapping("/{noteId}/share")` — sharing `PublishNoteUseCase` 주입·호출. `@ResponseStatus(CREATED)` + 기존 `requireMemberId` 재사용

**테스트 3종** (2026-06-01 작성 완료 — 컴파일 통과, 실행 검증은 CI 이관)

- [x] 컨트롤러 — `share_delegates`(위임) / `share_memberIdNull_rejected`(401)
- [x] 서비스 단위 — 스냅샷·닉네임 박제 / composeBody 4섹션 / confirmFalse·미저장(DRAFT)·중복공유 거부(각 save 미호출)
- [x] 통합 — `existsByNoteId` + `note_id` UNIQUE 제약(중복 공개 차단)
- > ⚠️ `gradlew test`는 한글경로(`프로젝트 - 2`) + Gradle9 워커 argfile 인코딩 이슈로 **로컬 CLI 실행 불가**. CI(Linux)에서 검증. 메인 컴파일·bootJar은 정상.

**dev-console** (2026-06-01 완료)

- [x] `static/dev-console.html` 신규 — X-Dev-User-Id + "나눔 공개" 섹션 + 재사용 `call()/show()` (화/목 섹션 덧붙이기 쉽게)
- [x] `DevSecurityConfig`에 frameOptions sameOrigin 1줄 추가(H2 콘솔 흰화면 방지, dev 전용)
- [x] 서버 부팅·배선 검증(직접 `java -jar dev`): dev-console 200 / share 404(노트없음)·401(미인증). **201은 시드(member+SAVED노트) 후 지민 눈 확인**

**핵심 개념 확인 (SKILL 규칙 4)**

- [x] 스냅샷 원자성 — 발행 시점 값 박제 → 원본 수정·닉네임 변경에 무영향
- [x] cross-domain UseCase 호출 — note.web → sharing.**api** 포트만, internal 직접 접근 금지(ArchUnit)

**마무리**

- [x] 배선·테스트작성·dev-console 검증 + todo/워크플로우 갱신
- [ ] 리포트(`reports/2026-06-01_*.md`) + commit·push (하루 마감 때, 사용자 동의 후)

---

### 화요일 (2026-06-02) — 좋아요 + 공유글 삭제 (작은 기능 2개)

> 둘 다 SharingPostController에 얹는 가벼운 기능. **한 브랜치 = 한 기능**이라 브랜치 2개로 분리.

#### A. 좋아요 `POST·DELETE /api/v1/sharing-posts/{postId}/like` — ✅ 2026-06-01(월) 당겨서 완료

> 결정: **likeCount = ② COUNT 재계산** · 중복 **409 DUPLICATE_LIKE**(S0004 신규) · 취소 **멱등 204**. 브랜치는 `feature/sharing-write`(나눔 쓰기 묶음, per-feature 분리 아님).

- [x] `ToggleLikeUseCase.like(LikeResponse)/unlike(void)` + `SharingPostService.like/unlike`(findByIdAndStatus 재사용)
- [x] `PostLike.of` · `SharingPost.syncLikeCount` · `PostLikeRepository` 보강(existsBy·countBy·deleteBy)
- [x] `SharingPostController` `@PostMapping("/{postId}/like")`(201) + `@DeleteMapping`(204) — **지민 직접**
- [x] 중복 좋아요 409 / 미존재 취소 멱등
- [x] 테스트 11개(서비스5·컨트롤러4·통합2) 컴파일 통과 + dev-console "좋아요" 섹션 + 부팅·배선 검증(201/409/204)

#### B. 공유글 삭제·숨김·되돌리기 — ✅ 2026-06-02 완료 (`feature/sharing-post-delete`)

> 팀 상의로 범위 확정: 삭제 + hide + **show(되돌리기, 04 신규)** 모두 v1 포함, **본인만**(관리자는 admin 이후). origin/dev 최신 기반 새 브랜치(어제 #191 머지로 sharing-write는 stale).

**구현**

- [x] `DeleteSharingPostUseCase`(delete) · `SharingPostVisibilityUseCase`(hide/show) 신규 포트
- [x] `SharingPostService` — `findOwnedPost`(조회+본인검증) + delete/hide/show, 멱등은 서비스·전이검증은 엔티티
- [x] `SharingPost.delete/hide/show()` — 상태 전이 + DELETED는 `INVALID_STATUS_TRANSITION`(409)
- [x] `SharingPostController` `@DeleteMapping`(삭제) + `@PatchMapping`(/hide, /show) 3개 + 관리자권한 TODO 주석
- [x] 04 §4.4.6에 show 추가(로컬 캐시 — gitignore, SSoT 문서저장소 별도 반영 필요)

**테스트**

- [x] 서비스 단위 10(정상·403·404·멱등·DELETED→hide/show 409) / 컨트롤러 6(위임·401) — compileTestJava 통과

**dev-console + 스모크**

- [x] "나눔 글 관리" 섹션(숨김·되돌리기·삭제) 추가
- [x] **Docker(MySQL) 스모크 통과** — 숨김/되돌리기/삭제 204, 멱등 204, 타인 403, 없음 404, 삭제후숨김 409

**핵심 개념 확인**

- [x] 멱등성 — 같은 요청 두 번도 안전 (목표 상태면 서비스가 조용히 통과)
- [x] 소프트 삭제 — status 전이로 처리(행 보존)
- [x] Rich Domain Model — 상태 변경 규칙을 엔티티에 캡슐화(Tell Don't Ask)

---

### 수요일 (2026-06-03) — 🛌 공휴일 (작업 없음)

---

### 목요일 (2026-06-04) — 댓글 — ✅ 2026-06-01(월) 당겨서 완료

> 결정: **평면 댓글(대댓글 X, parentId 항상 null·v2용)** · 본문 필드 **`body`**(04 명세 일치) · commentCount **COUNT 재계산** · 삭제 **soft delete·본인만 403·멱등** · 댓글OFF **409** · 닉네임 **실시간 조회**(getMemberPublic) · **새 CommentService + CommentController**.

- [x] **`CommentRepository` 신규** — `findBySharingPostIdAndIsDeletedFalseOrderByCreatedAtAsc` · `countBySharingPostIdAndIsDeletedFalse`
- [x] DTO — `CommentCreateRequest`(@NotBlank @Size body) · `CommentResponse`(+nickname·ownedByMe) · `CommentListResponse`(페이징)
- [x] `CommentUseCase`(create/list/delete) + **`CommentService`** 구현
- [x] `CommentController`(`/api/v1`) — POST·GET `/sharing-posts/{id}/comments` · DELETE `/comments/{id}` — **지민 직접**
- [x] 정책: 댓글OFF 409 / 본인만 403 / soft delete 멱등 / commentCount 재계산 / `Comment.of`·`markDeleted`·`syncCommentCount`
- [x] 테스트 16개(서비스8·컨트롤러6·통합2) 컴파일 통과 + dev-console "댓글" 섹션
- [x] 핵심 개념: 댓글 ON/OFF 게이트(UI+서버 이중 방어) · soft delete(행 보존·멱등) · 닉네임 박제 vs 실시간 조회

---

### 금요일 (2026-06-05) — 신고 `POST /api/v1/reports` (F-10, §4.4.7) + W3 마감

> 브랜치: `feature/report-create`. W3에서 가장 무거움 — **테이블 신규 + 명세 정합**. 착수 전 협의 필수.

**착수 전 협의 (아침 제일 먼저)**

- [ ] **reports 테이블 V14 이승욱 협의** (V13 전례) — `target_type`·`target_id`·`reason`·`detail`·`status`·reporter(member)·`created_at`·`reviewed_at`. (reporter, target_type, target_id) 유니크로 중복신고 차단
- [ ] **명세 정합** — Report 스켈레톤의 `share_snapshot_id` → 04 `targetType`(POST/COMMENT/AI_QA_REQUEST/AI_ASSET)+`targetId`로 교체. status enum 04 기준(RECEIVED/REVIEWING/RESOLVED/REJECTED), 스켈레톤의 PENDING/... 폐기

**신규 / 구현**

- [ ] `V14__create_reports.sql` (협의 결과 반영)
- [ ] `Report` 엔티티 + `ReportStatus` enum(RECEIVED 기본)
- [ ] `ReportCreateRequest`(targetType·targetId·reason·detail) + `ReportResponse`
- [ ] `ReportRepository`(existsByReporterId+target, save) + `ReportService`(CreateReportUseCase: 중복검증 + 대상 존재검증)
- [ ] `ReportController @PostMapping("/api/v1/reports")` 배선

**테스트 3종**

- [ ] 단위(중복신고 409·정상접수·잘못된 targetType) / 컨트롤러(201·401·409) / 통합(유니크 제약·status 기본 RECEIVED)

**dev-console**

- [ ] "신고" 섹션 추가 (targetType·targetId·reason 버튼) → H2 reports 행 확인

**W3 마감**

- [ ] W3 종료 자기 점검 (아래 섹션)
- [ ] 주간 회고 — `reports/2026-W3_주간회고.md` 작성
- [ ] 문서 commit + push

---

## Flutter 섹션 (디자인 확정 후 — W3 일정 외)

> 디자인 확정 시 W3 후반 또는 W4에 진행. 백엔드 우선 원칙(🅐).

- [ ] **S-01** — 나눔 피드 (`GET /sharing-posts` 연동)
- [ ] **S-02** — 나눔 상세 (좋아요·댓글·신고 UI)
- [ ] **S-03** — 나눔 작성/설정 (공개·공개취소·삭제, `confirmNicknamePublic` 필수)
- [ ] **N-01 달력 탭** — `GET /me/meditation-calendar`(이지윤 API) 연동, 막히면 Mock 우회

---

## W3 종료 시 자기 점검 (금요일 마무리)

- [ ] 나눔 쓰기 5기능 + 신고 PR이 모두 생성/머지 흐름에 올라감
- [ ] 각 PR에 테스트 3종(단위·슬라이스·통합) 포함
- [ ] dev-console.html에 5개 섹션(공개·좋아요·삭제·댓글·신고) 모두 동작 확인
- [ ] reports 테이블 V14 이승욱 협의·반영 완료
- [ ] 발행 로직에서 nickname_snapshot이 실제로 채워지는지 확인 (W2 부채 해소)
- [ ] W4 첫 작업(Flutter·외부공유) 시작 조건 점검 — 디자인 확정 여부

---

## 진행률 (자동 계산)

> 매일 마무리 시 갱신. `완료 / 총 체크박스` 형식.

- 착수 전 정리: ✅ 완료
- 월(공개): ✅ 완료 (2026-06-01)
- 좋아요: ✅ 완료 (2026-06-01 당겨옴)
- 댓글: ✅ 완료 (2026-06-01 당겨옴)
- 공유글 삭제: ⏸️ 범위 밖(별도) / 신고: ✅ dev 완료(#140)
- **W3 백엔드 나눔 쓰기**: **공개·좋아요·댓글 3/3 완료** → PR 예정. (공유글삭제·외부공유는 별도/W4)

---

## 참고 문서

- 요구사항: `doc/standards/07_요구사항_정의서.md` §F-10 (닉네임 나눔)
- API 계약: `doc/standards/04_API_명세서.md` §4.3.8(공개)·§4.4(나눔/댓글/좋아요/신고)
- ERD: `doc/standards/02_ERD_문서.md` §sharing_posts·comments·post_likes·reports
- 시퀀스: `05_시퀀스_다이어그램.md` (공개 스냅샷 원자성)
- 도메인 패키지 규칙: `CLAUDE.md` §3·§4 / 코드 컨벤션: `CODE_CONVENTION.md`
- 파일 레퍼런스: `reports/2026-05-28_note-sharing-파일레퍼런스_report.md` (+ 2026-05-29 정정 addendum)
- W2 워크플로우: `workflows/2026-W2_상세-워크플로우.md` / W2 회고: `reports/2026-W2_주간회고.md`
- 학습 규칙: `skills/SKILL.md`
