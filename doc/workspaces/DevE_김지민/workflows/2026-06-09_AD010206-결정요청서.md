# 2026-06-09 · 관리자 백엔드(AD-01/02/06 + 카카오) 결정 요청서

> 작성: 김지민 → 강태오(Lead)·이지윤·이승욱·강상민
> 목적: **AD-01 대시보드 / AD-02 QT관리 / AD-06 공지 / F1 카카오 로그인** 백엔드를 **6/10까지 구현**하려는데,
> 아래 6건이 코드 전에 정해져야 합니다. 추천안 있으니 **OK면 OK, 다르면 의견만** 주세요.
> 관련: [워크플로우](2026-06-09_admin-backend-AD010206-워크플로우.md) · [체크리스트](../todos/2026-06-09_admin-backend-AD010206-체크리스트.md)

---

> **읽는 법: 아래 "👤 사람별"에서 본인 이름을 찾으면 됩니다.**
>
> - `[결정]` = **답만** 주면 됨(OK / A / B). 코드 안 짜도 됨.
> - `[작업요청]` = **코드/작업 필요**(예: UseCase 하나 만들기). 또는 "김지민이 해도 됨" 승인.
> - `[확인]` = **정보만** 알려주면 됨(예/아니오).

## 👤 사람별 — 본인 이름만 보면 됨

### 강태오 (Lead + member 도메인)

- `[결정]` ① 공지를 **기존 notification 도메인 안**에 둬도 됨? (추천 B) → OK/아니오
- `[결정]` ② QT `status`는 **관리자 작업단계일 뿐, 실노출은 04:00 배치가 결정** — 이 의미로 OK? (추천 A)
- `[결정]` ③ 관리자 로그인 **카카오 재사용**(`/auth/kakao`, /oauth2 신설 X)? (추천 A)
- `[작업요청]` ④ member에 `GetMemberStatsUseCase` 노출 가능? (없으면 대시보드 4종만) — 또는 "김지민이 PR로 만들어도 됨" 승인
- `[작업요청]` ⑤ 공지 발행용 **전체 회원 id 목록** 제공 가능? (현 `send()`는 단건)

### 이지윤 (qt 도메인 공동 오너)

- `[결정]` ② QT 도메인에 `status` 컬럼 + admin 컨트롤러 추가 — **동의?** (의미는 위 ②와 동일)

### 이승욱·강상민 (MSA 리드)

- `[확인]` ⑥ **qt·notification·admin** 도메인 곧 MSA 추출 예정? (지금 ai·bible만 추출 중으로 보임 → 겹치면 충돌)

---

## ✅ 확정 결과 (2026-06-09 회신)

| # | 결정 | 비고 |
|---|---|---|
| ① 공지 위치 | ✅ **B** | notification 도메인 하위에 notices |
| ② QT status | ✅ **A**(status 컬럼) | 이지윤 동의 + **상태셋 제안** `active / hidden / pending_review / deletion_notified / removed` (운영자 모더레이션). 실노출은 04:00 배치 유지 |
| ③ 카카오 | ✅ **A** | `/auth/kakao` 재사용 |
| ④ member 집계 | ✅ **김지민 PR 승인** | member에 `GetMemberStatsUseCase` 추가(강태오 리뷰) |
| ⑤ broadcast | ✅ **김지민 PR 승인** | member에 "전체 회원 id 목록" UseCase 추가(강태오 리뷰) |
| ⑥ MSA 추출 | ✅ **당장 추출 X(안전)** | 강상민 해당없음 / 이승욱: qt·notification·admin은 Phase 2(미착수) → 모놀리식 작업 안전. ⚠️ **qt는 bible 소비자 HTTP 전환(Inc3d 완료) 진행 중 → AD-02 착수 전 이승욱과 협의.** 최종 admin→admin-service, qt→today-qt 추출 예정(나중) |

### 후속 반영
- **②** AD-02 `QtPassageStatus`를 이지윤 제안 5종으로 재설계(active/hidden/pending_review/deletion_notified/removed). 실노출은 04:00 배치.
- **④⑤** member 집계·broadcast UseCase를 김지민이 member 도메인에 추가(강태오 리뷰) → AD-01 member 지표·AD-06 발행 언블록.
- **⑥** AD-02 착수 전 **이승욱과 qt(Inc3d) 충돌 협의** 필수.

### 🔄 2026-06-10 변경 — ③ 카카오 재논의
> MSA가 **service-user / admin-server**로 더 분리되며, 이승욱이 관리자 카카오 인증을 **신규 `POST /api/v1/admin/auth/kakao`** 로 발급하는 [계약](2026-06-10_admin-kakao-auth-contract.md)을 제안. 이는 위 **③A(기존 `/auth/kakao` 재사용, 신규 X)** 와 **충돌**.
> - **응답 형태 5개**(키 `admin`/`member` 구분 · `adminRole` 단일 · refreshToken body · 403 ErrorCode 표시 · 만료 30분/14일)는 **2026-06-10 FE 합의 완료**.
> - **엔드포인트 경로**: ✅ 2026-06-10 팀 결정으로 **신규 `admin/auth/kakao` 채택 확정**(③ 대체).

---

## ⚡ 전체 표 (참고 — 위 "확정 결과"가 최신)

| #   | 유형     | 무엇을 (쉽게)                                        | 받을 사람     | 추천      | **결정** |
| --- | -------- | ---------------------------------------------------- | ------------- | --------- | -------- |
| ①   | 결정     | 공지를 새 도메인 vs **기존 notification 안**         | 강태오        | **B**     | ✅ B   |
| ②   | 결정     | QT `status`="관리자 단계", 실노출은 04:00 배치 — OK? | 강태오·이지윤 | **A**     | ✅ A(+상태셋)   |
| ③   | 결정     | 관리자 로그인 **카카오 재사용**(/oauth2 신설 X)      | 강태오        | **A**     | ✅ A   |
| ④   | 작업요청 | `GetMemberStatsUseCase` 노출(없으면 4종만)           | 강태오        | 요청      | ✅ 김지민 PR   |
| ⑤   | 작업요청 | 공지 발행용 **전체 회원 id 목록** 제공               | 강태오        | 요청      | ✅ 김지민 PR   |
| ⑥   | 확인     | qt·notification·admin **곧 추출 예정?**              | 이승욱·강상민 | 안전 확인 | ✅ 당장 X·qt협의   |

---

## 📋 항목별 설명 (왜 필요한지)

### ① 공지(notices) 위치 — 받을 사람: **강태오**

- 공지 기능은 "공지 원본 저장"이 필요한데 그 테이블·코드가 아직 없습니다.
- **추천 B**: 기존 `domain/notification`에 이미 **`SendNotificationUseCase`(알림 발송)** 가 있어, 공지 발행 시 그걸 그대로 호출합니다. → 같은 도메인에 두면 **새 도메인 등록(07/03 문서 보강) 불필요**, 일정 가벼움.
- A안(새 `domain.notice`)도 가능하나 도메인 목록 등록(문서)이 따라옵니다.

### ② AD-02 QT status 의미 — 받을 사람: **강태오·이지윤**

- AD-02 화면이 "발행/숨김" 버튼을 기대해서 `qt_passages`에 `status(DRAFT/PUBLISHED/HIDDEN)` 컬럼을 추가하려 합니다.
- ⚠️ 단 CLAUDE.md §6 = QT 사용자 노출은 **04:00 배치 + qtDate**가 결정. → 충돌 안 나게 **"status는 관리자 작업 단계(초안→발행승인)일 뿐, 실노출 토글이 아니다"** 로 의미를 못박고 싶습니다. **이 해석 OK?**
- (qt 도메인을 수정하므로 오너 이지윤·강태오 동의 필요)

### ③ 카카오 웹 로그인 — 받을 사람: **강태오**

- 지금 관리자 웹 로그인은 dev 토큰 수동입력(임시). 이걸 진짜 로그인으로 정식화.
- **추천 A**: 앱과 동일하게 `/api/v1/auth/kakao` 재사용(서버 `/oauth2/**` 신설 안 함 = §5 규칙 유지). DRAFT PR #319(B안 /oauth2)는 교체.

### ④⑤ member UseCase 2건 — 받을 사람: **강태오(member 오너)**

- ④ 대시보드 회원 지표: `GetMemberStatsUseCase` → `MemberStats(totalMembers, newToday, newThisWeek)`. **없으면 대시보드는 4종(검증대기·미처리신고·오늘QT·최근감사)만** 먼저 갑니다.
- ⑤ 공지 발행 broadcast: 현 `send()`는 단건이라 전체 회원에게 보내려면 **회원 id 목록**이 필요.
- → 강태오가 만들어주거나, **김지민이 PR로 기여 + 강태오 리뷰**도 가능(member는 강태오 도메인이라 사전 동의 필요).

### ⑥ MSA 추출 일정 — 받을 사람: **이승욱**

- AD-01/02/06은 **qt·notification·admin** 도메인을 건드립니다. 지금 추출 중인 건 ai·bible로 보여 안전하지만, 이 도메인들이 **곧 추출 예정인지** 확인 필요(겹치면 작업 충돌).

---

## ✅ 결정 후 반영 위치 (김지민 메모)

- ①→ AD-06 폴더 위치 확정 · ②→ AD-02 status 설계 마감 · ③→ F1 착수 · ④⑤→ 대시보드 member 지표·AD-06 발행 · ⑥→ 작업 안전 확인
- 결정 오면 위 표 "결정" 칸 채우고 워크플로우/체크리스트 반영.

> **참고**: 결정 없이도 **AD-01 4종**과 **F1(③ 확정 시)** 은 먼저 착수 가능. ②③·④⑤는 AD-02·06·member 지표를 막고 있어 **빠른 결정이 일정에 직접 영향**.
