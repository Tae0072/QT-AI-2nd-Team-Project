# 작업 리포트 — 2026-05-28 갭 분석: 명세엔 있으나 담당 없는 기능

> **담당**: 김지민
> **목적**: 바이블서버 팀장 공백으로 생긴 "구현하기로 했는데 담당자가 없는 기능"을 찾아 재분담 자료로 정리
> **방법**: 04 API 명세(88개 엔드포인트) × 구현_리스트.md(3인 분담) × 07 F-ID + **실제 코드(컨트롤러 배선) 검증**
> **전제(확인됨)**: 팀=바이블서버 3인(이승욱·김지민·이지윤). 관리자=V2. AI v1 여부 미정.

---

## ⭐ 핵심 결론

처음엔 "ai/study 통째로 고아"로 보였으나, **실제 코드를 전수 확인한 결과 정반대.** ai는 강상민이 대부분 구현, study도 서비스 구현됨. **진짜 무주공산은 튜토리얼(F-07) 하나뿐.**

> **교훈: "담당 없음"은 문서가 아니라 코드(컨트롤러 배선 `@RestController` vs `// TODO`)로 검증해야 정확하다.**

---

## 분류

### 🔴 진짜 고아 (명세 O, 코드 X, 담당 X)
| 기능 | 엔드포인트 | F-ID | 비고 |
|---|---|---|---|
| **첫 실행 튜토리얼** | `GET /api/v1/tutorial`, `POST /api/v1/me/tutorial/complete` | F-07 | 코드·구현_리스트 어디에도 없음. **백엔드+Flutter(A-04 가이드 화면) 둘 다** 누락 |
| (경미) 세션 조회 | `GET /api/v1/me/session` | F-04 | `GET /me`로 대체 가능성 — 확인 필요 |

> 참고: 이승욱 A-04 화면이 "온보딩 완료 환영"으로 잡혀 있는데, 명세 A-04는 "QT 설명·사용순서 안내"라 별개. 튜토리얼 가이드 화면도 분담에서 빠짐.

### 🟠 owner 있으나 컨트롤러 미배선 (= 미완, 고아 아님)
담당자에게 "왜 컨트롤러가 아직 TODO인지 + 마무리" 핑.

| 기능 | 엔드포인트 | 컨트롤러 상태 | 담당(추정) |
|---|---|---|---|
| 해설 조회 | `GET /qt/{id}/study-content` | `StudyController` **TODO** | 김태혁/이지윤 |
| 시뮬레이터 조회 | `GET /qt/{id}/simulator-clips/{clipId}` | `StudyController` **TODO** | 김태혁 |
| AI Q&A(사용자) | `POST/GET /ai/qa-requests` | `AiController` **TODO** | 강상민 (system/admin은 구현됨) |
| 사용자 설정 | `GET/PATCH /me/settings` | UseCase/DTO만 있고 컨트롤러 없음 | 이승욱 |

### 🟡 김지민 본인 W3 (아직 시작 안 함 — 정상)
`POST /notes/{id}/share`(공유), 좋아요(`/like`), 댓글(`/comments`), 내 나눔(`/me/sharing-posts`), 공개중단(`/hide`), 나눔 삭제, 신고(`POST /reports`, ReportController TODO).

### ⚠️ 토큰 공유(Share) — "미아"가 아니라 명세에 없는 죽은 코드 (제거 검토)
- 코드: `Share`/`ShareSnapshot`/`SharingService`/`SharingController`(`/api/v1/shares`) + Create/Get/Revoke Share UseCase + client mock 4개 — **전부 TODO 스켈레톤**
- **삭제 근거 3가지:**
  1. **04 명세에 없음** (`/shares`·`shareToken` 어디에도 없음)
  2. **07 비회원 접근 금지 위배** (line 81 "비회원 접근 허용 안 함", line 1107 제외항목 "비회원 Read-only 접근"). 토큰 조회 `GET /token/{token}`은 비로그인(`permitAll`) 설계 → 정면 충돌
  3. **앱 전용이라 링크 열 웹페이지가 없음** (우리는 Flutter 앱만 개발 → 토큰 링크를 받아 볼 화면 자체가 없음)
- 명세의 "외부 공유"(N-04)는 `share_plus` **클라이언트 공유**(카톡 등 외부 앱에 텍스트/이미지 전송, 백엔드 불필요)이고 이미 김지민 Flutter 담당. 토큰 백엔드와 무관.

### ⚪ V2 의도적 이연 (고아 아님)
관리자 웹+백엔드 대부분(AdminController엔 `GET /admin/me`만), 감사로그(AuditController TODO), 절 다중선택.

---

## 명세/정책 위반 스캔 결과 (코드 전수 grep)
금지 기술·데이터·기능 패턴 검사 → **위반은 토큰 공유 하나뿐.**

| 검사 | 결과 |
|---|---|
| SSE / `/ai/sessions` / Flux | ✅ 없음 |
| Kafka·Elasticsearch·Chroma·LangChain·K8s | ✅ 없음 |
| Anthropic SDK (DeepSeek만 허용) | ✅ DeepSeek만, "Anthropic 금지" 주석까지 |
| 금지 번역본(개역개정/ESV/NIV/두란노) | ✅ 없음 (테스트 메서드명·RSA키 오탐만) |
| permitAll(비로그인) | ✅ `/auth/kakao`·`/refresh`(정상)+dev/local만 |
| **토큰 공유 비회원 접근** | ⚠️ 유일한 위반 |

---

## 회의 안건 (3개) + 누구에게

| # | 안건 | 누구에게 |
|---|---|---|
| 1 | **튜토리얼(F-07) 담당 지정** (백엔드+Flutter A-04) | 강태오(Lead) → member 영역이라 이승욱 유력 |
| 2 | **study/ai/settings 컨트롤러 미배선 마무리 확인** | 김태혁(해설·시뮬레이터)·강상민(Q&A)·이승욱(설정) |
| 3 | **토큰 공유(Share) 처리 결정** (삭제 vs 명세·정책 개정) | ① 이승욱(나눔 Entity 제작자·의도 확인) → ② 강태오(삭제 최종 결정) |

> 전체 Lead = 강태오(@Tae0072) — V1/V2 결정·규칙·master 머지. 바이블서버 3인엔 내부 팀장 없음.

---

## 참고
- 상세 분석 원본: `~/.claude/plans/abundant-splashing-lantern.md`
- 동반 리포트: `2026-05-28_note-sharing-파일레퍼런스_report.md` (69파일 역할·필드·기대값)
