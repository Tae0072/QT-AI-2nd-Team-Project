# 감사 조회 뷰 확장 결과 리포트 (#649 후속)

- 작성일: 2026-06-15
- 작성자: 김지민 (DevE)
- 브랜치: `feature/admin-audit-explanation-visibility` (base: dev c3317c53)
- 관련 F-ID: F-06(관리자 운영·감사), F-14
- 워크플로우: `doc/workspaces/DevE_김지민/workflows/2026-06-15_감사조회-뷰확장-워크플로우.md`

## 1. 한 일 (백엔드 전용)
PR #649 리뷰 WARN("감사 조회 가시성")을 처리. 관리자 해설 생성 트리거 로그(`AI_EXPLANATION_GENERATE_REQUEST`, target=`QT_PASSAGE`)가 AD-07 감사 조회에 노출되도록 확장.

| 변경 | 파일 |
|---|---|
| `LIST_WHERE` targetType 등치 → null 허용(`(:targetType is null or ...)`) | `AuditQueryRepository.java` |
| `AI_ACTION_TYPES`에 `AI_EXPLANATION_GENERATE_REQUEST` 추가 / `resolveTargetType` 미지정 시 null(대상 무관)·지정 시 `{AI_GENERATED_ASSET, QT_PASSAGE}` 검증 | `AuditQueryService.java` |
| (#649 후속 정리) 시드 `seedForPassage`의 `context==null` 죽은/모순 분기 제거 | `AiDailyQtVerseExplanationSeedService.java` (admin-server·service-ai) |
| 가시성 테스트 추가 | `AuditQueryRepositoryTest.java` |

**FE 변경 없음** — AD-07 `AuditLogsPage`는 targetType 미전송 + actionType 자유입력이라 기본 목록이 자동으로 QT_PASSAGE 로그까지 노출(컬럼 "QT_PASSAGE #35").

## 2. 검증
| 항목 | 결과 |
|---|---|
| `AuditQueryRepositoryTest`(신규 가시성 + 기존 projection) | ✅ |
| `AdminDashboardAuditLogServiceTest` / `AdminAiExplanationControllerTest` / `AiServiceGenerateExplanationTest` 회귀 | ✅ |
| `:service-ai:compileJava`(시드 변경) | ✅ |

하위호환: targetType 명시 조회는 기존과 동일 동작(`(:targetType is null or =)`). 기존 AI 자산 전용 조회 깨지지 않음.

## 3. 남은 리스크 / 후속
- 04 API 명세 audit `targetType` 허용값 명문화(문서, 경미).
- 시뮬레이터(QT 영상) 전용 관리 화면(B-1) — 팀원 분배/다음.

## 4. PR
- 제목(안): `feat(admin): 감사 조회 뷰에 해설 생성 트리거(QT_PASSAGE) 노출 (F-06)`
- 변경 유형: feat (BE) — #649 후속(리뷰 WARN4)
