# 관리자 시뮬레이터 관리 화면 A1(조회+숨김) 결과 리포트

- 작성일: 2026-06-15
- 작성자: 김지민 (DevE)
- 브랜치: `feature/admin-web-simulator-management` (base: dev 750a0946)
- 관련 F-ID: F-06(관리자 운영), F-12(시뮬레이터)
- 워크플로우: `doc/workspaces/DevE_김지민/workflows/2026-06-15_관리자-시뮬레이터관리화면-A1-워크플로우.md`

## 1. 한 일
기능격차 B-1(시뮬레이터 전용 관리 화면 부재) 해소 — SimulatorClip(study) **조회+숨김** 화면 신설(AD-14). 대시보드 시뮬레이터 CTA의 임시 연결(#647)을 전용 화면으로 교체. 게시(Publish)는 후속.

| 변경 | 위치 |
|---|---|
| `SimulatorClipRepository.findForAdmin`(상태/본문 optional, 페이지) | service-bible(원본) + admin-server(동기화) |
| `ListAdminSimulatorClipsUseCase` + Query/Response/Item DTO | 〃 |
| `SimulatorClipPublishService` list 구현(상위 N·상태 파싱) | 〃 |
| `AdminSimulatorClipController`: `GET /api/v1/admin/simulator-clips`(list) / `POST /{aiAssetId}/hide` | admin-server 단독 |
| hide 감사로그 `SIMULATOR_CLIP_HIDE`(target=AI_GENERATED_ASSET) + `AuditQueryService` allowlist 추가 | admin-server |
| FE: `api/simulatorClips.ts`, `SimulatorClipsPage`(목록·상태필터·숨김), 메뉴 AD-14, 라우트, 대시보드 CTA→`/simulator-clips?qtPassageId=` | admin-web |

권한: ROLE_ADMIN + admin_role REVIEWER(SUPER_ADMIN 우월권). 목록은 sceneScript 원문 미노출(메타만).

## 2. 검증
| 항목 | 결과 |
|---|---|
| `AdminSimulatorClipControllerTest`(list 200·hide 200+감사·admin_role부족 403) | ✅ |
| `AdminServerSecurityTest`(신규 엔드포인트 미인증 401/403·비ADMIN 403) | ✅ |
| `SimulatorClipPublishServiceTest`(기존 회귀, 동기화 무손상) | ✅ |
| `:service-bible:compileJava` / `:admin-server:compileJava` | ✅ |
| admin-web `npm run build` + `npm test`(contracts) | ✅ |

## 3. 남은 리스크 / 후속
- **게시(Publish)** — AD-03 승인본 sceneScript 연동. 별도 후속 PR.
- **B: QtVideoClip(qtvideo) 관리 화면** — admin BE 전무, 별도 브랜치/팀원.
- 목록 쿼리는 admin-only read지만 동기화 규칙대로 service-bible 원본에도 둠(service-bible엔 호출 컨트롤러 없음 — 동기화 대칭 유지).

## 4. PR
- 제목(안): `feat(admin): 시뮬레이터 클립 관리 화면(조회+숨김) AD-14 (F-06/F-12)`
- 변경 유형: feat (BE service-bible+admin-server 동기화 + admin 컨트롤러 + FE)
