# 결과 리포트 — 나눔 피드 낙관적 좋아요 (③)

| 항목 | 내용 |
| --- | --- |
| 담당자 | 김지민 (flutter-app) |
| 브랜치 | `feature/sharing-feed-card-optimistic-like` (base `dev`) |
| 관련 F-ID | F-10(닉네임 나눔) |
| workflow | doc/workspaces/DevE_김지민/workflows/2026-06-12_나눔피드-낙관적좋아요-워크플로우.md |
| 상태 | 구현·테스트 완료, PR 진행 예정 |

## 한 일
- 카드 레이아웃은 이미 `PostCard`로 적용돼 있어, ③의 실제 작업은 **좋아요 낙관적 업데이트**였다.
- `sharingPostsProvider`를 `AsyncNotifier`(`SharingFeedNotifier`)로 전환하고 `toggleLike(postId)`를 추가 —
  즉시 로컬 갱신(낙관적) + 실패 롤백. 전체 재조회(invalidate) 없이 해당 글만 바뀐다.
- `SharingPostItem.copyWith`, `PostCard`의 하트 탭(onLike), 피드 배선 + 실패 안내 스낵바.

## 검증
- 신규 `sharing_feed_notifier_test` 3건(낙관적 +1/−1·서버 호출·롤백) + 기존 `post_card_test` 3건 통과.
- `flutter analyze` 클린 + `flutter test` 전체 **231건 통과**.

## 남은 리스크 / 후속
- 상세(S-02) 좋아요는 아직 invalidate 방식 — 피드(S-01)만 낙관적. 상세 낙관적은 후속 검토.
