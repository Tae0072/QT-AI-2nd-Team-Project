# 관리자 웹 QA 리포트 FE 수정 — PR1 작업 결과

- **작성일**: 2026-06-12
- **작성자**: 김지민
- **브랜치**: `fix/admin-web-qa-assets-filter-modal` (base: `dev`)
- **상위 워크플로우**: `doc/workspaces/DevE_김지민/workflows/2026-06-12_관리자웹-QA리포트-FE수정-Workstream-B.md`
- **근거 검수**: 관리자 웹 AD-01~AD-10 비즈니스 검수(2026-06-12) — P1·P3 항목
- **작업 성격**: admin-web React/TypeScript QA 후속 수정 (백엔드 변경 없음)
- **관련 F-ID**: F-06(관리자 운영 기능), F-14(AI 산출물 품질 관리)

---

## 1. 작업 목표

QA 비즈니스 검수에서 나온 다음 두 건을 처리한다.

| 우선도 | 항목 | 검수 발견 |
|---|---|---|
| P1 | AD-03 기본 status 필터 | 대시보드 "AI 검증 대기 N"과 AD-03 목록(`데이터 없음`) 불일치 — 진입 시 전체 조회라 검증 대기 산출물이 묻힘 |
| P3 | antd `destroyOnClose` deprecation | 콘솔 경고 `Modal destroyOnClose is deprecated. Please use destroyOnHidden instead.` |

두 항목 모두 순수 FE 버그픽스·호환성 수정이고 비즈니스 로직 변경이 없어 한 PR로 묶는다.

---

## 2. 변경 내역

### 2-1. AD-03 기본 status 필터 (P1)

**파일**: `admin-web/src/pages/AiAssetsPage.tsx`

- `usePagedList` 초기 파라미터에 `status: 'VALIDATING'` 추가
- `useState` status 초기값 `undefined` → `'VALIDATING'`
- `onReset` 도 `'VALIDATING'`로 통일 (초기화 시 기본 필터로 복귀)

진입 시 검토 대상인 `VALIDATING` 산출물만 표시되어 대시보드 "검증 대기" 지표와 동선이 이어진다.

### 2-2. antd destroyOnHidden 일괄 교체 (P3)

**파일**: 5개 / 13곳 — `destroyOnClose` → `destroyOnHidden` 단순 prop 치환 (동작 동일)

| 파일 | 곳 |
|---|---|
| `AiAssetsPage.tsx` | 4 |
| `AiEvaluationsPage.tsx` | 4 |
| `PraiseSongsPage.tsx` | 2 |
| `ReportsPage.tsx` | 2 |
| `AiChecklistsPage.tsx` | 1 |

---

## 3. 검증 체크리스트

### 자동 검증 (완료)

- [x] `grep -rn destroyOnClose src/` → 0건
- [x] AD-03 `VALIDATING` 기본필터 3곳 적용(usePagedList 초기 · useState 초기 · onReset)
- [x] `destroyOnHidden` 13곳(5파일) 적용
- [x] `npm run typecheck` 통과
- [x] `npm run build` 통과 (chunk size 경고는 기존 사항)

### 수동 검증 (머지 후 로컬 확인 권장)

- [ ] AD-03 진입 시 `status=VALIDATING` 행만 표시
- [ ] "초기화" 클릭 시 `VALIDATING` 으로 복귀(전체 조회로 풀리지 않음)
- [ ] 상태 Select 드롭다운에 "검증중(VALIDATING)" 선택 표시
- [ ] 임의 모달(승인/반려/재생성/평가추가/신고/찬양/체크리스트) 닫았다 다시 열면 폼 필드 초기화
- [ ] 브라우저 콘솔에 `destroyOnClose` deprecation 경고 사라짐

---

## 4. 범위 밖(이 PR에서 다루지 않음)

- 대시보드 CTA(P2) · AI 모니터링 가독성(P2) → **PR2 `feat/admin-web-qa-dashboard-monitoring`**
- 찬양 페이지네이션(P1) → `feature/praise-admin-crud` 담당(머지 후 수동 확인만)
- AD-05 찬양 404성 오류 문구(P1) → 서버/API 매핑 확인 필요, 별도 처리

---

## 5. 보안/정책 체크

- FE 표시 개선만 — 서버 API 호출 로직·권한 체크 변경 없음
- 민감 데이터(password/token/key) 로그·코드 노출 없음
- 산출물 원문 비노출 정책(CLAUDE.md §7) 영향 없음 (목록은 메타데이터만)

---

## 6. PR 정보

- **대상**: `dev`
- **제목**: `fix(admin-web): AD-03 기본 status 필터 + antd destroyOnHidden 일괄 교체 (F-06/F-14)`
- 상세 본문은 PR 설명 참조
