# QT-AI 전체 구현현황 분석 (재분배용)

> **작성:** Lead 강태오 · **날짜:** 2026-06-02
> **방법:** 작업 레포(`qtai-server` + `flutter-app`) 전수 분석 + 팀원별 공식일정표(v0.4)·실행가이드·6/1 일정표 대조. 각 작업을 `완료/진행중/미구현/스텁/오류`로 판정.
> **GitHub 핸들:** @Tae0072=강태오 · @ij447504-source=이지윤 · @LeeSeung-Wook=이승욱 · @rmfdnjf98=김지민 · @sangminka=강상민 · @xogurrh012=김태혁

---

## 0. 한눈 요약

| 영역 | 상태 |
|---|---|
| **백엔드 핵심 도메인** | member·note·notification·praise·report·sharing(좋아요/댓글)·ai(생성·검증 파이프라인) **대부분 완료** |
| **백엔드 미구현/스텁** | AI 사용자 Q&A(F-15)·qt CRUD·audit 조회·study 목록·sharing 토큰공유·bible 검색/장목록 |
| **🔴 즉시 버그** | `bible/books`·`bible/verses` **런타임 500**(enum-시드 불일치) + `bible_verses` 시드 자체 부재 |
| **📱 Flutter** | QT·성경·노트·달력 화면 **0%**(디렉터리조차 없음), 관리자 웹 0% — **시연 최대 병목** |
| **중복/데드코드** | member settings UseCase 2쌍·qt simulator/study UseCase·report GetReport·sharing 토큰공유 라인 |

---

## 1. 🔴 즉시 수정 (demo-blocking)

| # | 문제 | 위치 | 영향 | 담당 |
|---|---|---|---|---|
| 1 | `bible_books` 시드 `testament='OT'/'NT'` vs `BibleBook.Testament` enum이 `OLD/NEW`만(@Enumerated STRING) | `db/migration/V7__seed_bible_books.sql` ↔ `bible/internal/BibleBook.java:38-40` | **`GET /bible/books`·`/bible/verses` 둘 다 500** | 이지윤 |
| 2 | `bible_verses` 시드 INSERT 자체가 없음(V8은 qt_passages 3건만) | `db/migration/` | 버그 1 고쳐도 **성경 본문 조회 데이터 0건** → 시연 불가 | 이지윤 |
| 3 | bible 테스트가 전부 mock/팩토리라 위 버그를 구조적으로 못 잡음 | `src/test/.../bible/` (flyway off) | 회귀 방지 공백 — 시드↔enum 통합 테스트 필요 | 이지윤 |

---

## 2. 🟡 중복 / 데드코드 (동작 무해, 정리 권장)

| 위치 | 문제 | 담당 |
|---|---|---|
| `member/api/GetMemberSettingsUseCase`·`UpdateMemberSettingsUseCase` | 실사용은 `GetSettingsUseCase`/`UpdateSettingsUseCase` — 미참조 데드 인터페이스 | 이승욱/강태오 |
| `qt/api/GetQtSimulatorUseCase`·`GetQtStudyContentUseCase` | 빈 TODO 데드(진짜는 `study.api`) | 강태오 |
| `report/api/GetReportUseCase` | 미참조 데드 인터페이스 | (report 공동) |
| `sharing` 토큰공유 라인 전체(`SharingController`/`SharingService`/`CreateShare`·`GetSharedSnapshot`·`RevokeShare`) | 닉네임 나눔(SharingPost)으로 대체돼 사문화된 스텁 → "정리 여부 Lead 결정 대기"(김지민 todo) | 이승욱/김지민/강태오 |
| `V9__create_notifications.sql` 주석이 `-- V6`로 오타 | 무해 | — |

---

## 3. 팀원별 상세

### 강태오 (Lead) — member/qt/admin/audit/DevOps/MSA/TTS

| 상태 | 항목 |
|---|---|
| ✅ 완료 | member 전체(auth/me/settings/dashboard/nickname/탈퇴), qt Today·passage 조회, 관리자 신고 처리 API(report 도메인), H2→MySQL, Docker Compose 시연스택, Testcontainers·컨텍스트 로드 가드, 통합 E2E 3종(Today QT·신고·시뮬레이터 상태), k6 성능 baseline, dev→master 릴리스, Feature Freeze/품질게이트 |
| 🔶 진행중 | 도메인 통합 E2E 전체 관통(로그인→…→대시보드), 성능 캐시 히트율 측정 |
| ⛔ 미구현/스텁 | **qt CRUD 5종**(Create/Get/ListMy/Update/Delete — 인터페이스 스텁), **audit 조회 API**(`AuditController` 전체 스텁, `ListAuditUseCase` 없음), **admin** GetStats/LookupMember/ModerateContent |
| 🅥2 | TTS 3건, MSA 분리 트리거·bible 도메인 분리 구상 (전부 미착수) |

### 강상민 — ai / 관리자 시큐리티

| 상태 | 항목 |
|---|---|
| ✅ 완료 | AI 산출물 자동 검증(`AiAutoValidationService`), 민감정보 차단(FORBIDDEN_FIELDS), 산출물 유출 정책(원문 미저장), 검증 참조 원문 비공개, System/Admin AI 생성·검증 파이프라인 전체, AI 관리자 권한 검사(REVIEWER/SUPER_ADMIN), SYSTEM_BATCH 검사 |
| ⛔ 미구현/스텁 | **사용자 F-15 Q&A 전무**(`AiController` 스텁, `RequestAiQa`/`GetAiQaResult`/`ReviewAiAsset` 구현체 0, 차단 테스트 0), **자동 생성 실패 재시도 미구현**(retry 0건, 실패 즉시 FAILED), **RUNNING job 복구 미구현**(QUEUED만 폴링), service_accounts 전역 토큰 필터(후속 분리) |
| ⚠️ 불일치 | 워커가 "04:00 KST cron"이 아니라 10초 fixedDelay 상시 폴링 + 00:05 시딩 cron. "04:00 워커" 전제는 코드와 불일치(04:00은 Today QT cache/study 책임) |
| ↪ 타도메인 | "승인 해설만 노출"·"admin_role 2차 검증"은 study/admin 소유(강상민 작업 아님) |

### 이승욱 — sharing 백엔드 / me/settings / Flutter 팀장

| 상태 | 항목 |
|---|---|
| ✅ 완료 | 좋아요/취소·댓글 작성/삭제·노트 나눔 공개(스냅샷)·me/settings(GET/PATCH) + 각 테스트, Flutter M-02 알림·M-03 찬양·M-06 설정·S-01 피드 |
| 🔶 진행중/부분 | Flutter S-02 상세 — 좋아요·댓글·신고가 "준비 중" SnackBar로 **API 미연결**(백엔드는 완성) |
| ⛔ 미구현/스텁 | **공유글 삭제 `DELETE /sharing-posts/{id}`**(좋아요 취소 DELETE와 혼동, 실제 없음), **외부 토큰 공유**(`/api/v1/shares` 라인 전체 스텁), **Flutter 나눔 작성 S-03 화면 없음** |

### 이지윤 — bible 백엔드 / QT·성경 Flutter

| 상태 | 항목 |
|---|---|
| ✅ 완료 | bible books/verses/getVerse 서비스 골격 + bible 단위·슬라이스 테스트, 성경 JSON 출처 검토표(문서, 단 승인 출처 0건 placeholder) |
| 🔴 오류 | **bible 조회 500**(섹션 1) + bible_verses 시드 부재 |
| ⛔ 미구현/스텁 | `ListChaptersUseCase`·`SearchBibleUseCase`(본문 TODO), 성경 구절 자동삽입(@), **시연 데이터 seed**(bible_verses 0건), bible 시드↔enum 통합 테스트, AI 서버 합치기(다음 주), **QT Q-01~06·성경 B-01~03 Flutter 0%**(디렉터리 부재) |

### 김지민 — note 백엔드 / 노트·달력 Flutter / 관리자 웹

| 상태 | 항목 |
|---|---|
| ✅ 완료 | note 조회/생성/수정/삭제/draft/카테고리·묵상 달력 API(백엔드), 나눔 피드 조회·좋아요·댓글 컨트롤러(본인 직접 작성) + 테스트, 자동 로그인→홈 진입(귀속은 이승욱일 수 있음) |
| ⛔ 미구현/스텁 | **Flutter 노트 N-01~04·달력 탭·편집 툴바·widget 테스트 0%**(`features/note`·calendar 부재), **관리자 웹 0%**(`admin-web` 디렉터리 부재), sharing 토큰공유 스텁(정리 대기) |
| 비고 | note 생성/publish는 이승욱과 분담/중복 정리된 공동 영역(#179 충돌 → A안 정리) |

### 김태혁 — 시뮬레이터 전담 (제작·연결만)

| 상태 | 항목 |
|---|---|
| ⚠️ 저작자 불일치 | 일정표가 "김태혁 담당"으로 적은 **simulator 백엔드(study 도메인)는 실제로 이지윤(05-28)·강태오(06-02)가 작성**. 김태혁의 origin/dev Java 커밋 0건 |
| 🔶 진행중(코드 외) | 6/1 본인 작업(12파츠 FK 스켈레탈 애니메이션·의상 레이어·Remotion 통합)은 **별도 애니메이션 프로젝트**에서 진행. 리포트 2건은 **미머지 브랜치 `xogurrh012-patch-1`**에만 존재 |
| ✅ 완료(타인 구현) | simulator 상태 모델·조회 API·상태 분기·E2E는 구현돼 있음(이지윤/강태오) |
| ↪ 연결 | 클립 산출물 서버 연결은 6/1 기준 강상민 담당 |

---

## 4. 📱 Flutter 전체 (시연 최대 병목)

| 화면군 | 상태 | 담당 |
|---|---|---|
| 온보딩(A-02 로그인·A-03 닉네임), 마이페이지 M-01~M-06, 나눔 S-01 피드 | ✅ 실구현 | 이승욱 |
| 나눔 S-02 상세 | 🔶 부분(좋아요·댓글·신고 API 미연결) | 이승욱 |
| **QT Q-01~06** | ⛔ 0% (features/qt 없음) | 이지윤 |
| **성경 B-01~03** | ⛔ 0% (features/bible 없음) | 이지윤 |
| **노트 N-01~04 + 달력** | ⛔ 0% (features/note 없음) | 김지민 |
| **나눔 작성 S-03** | ⛔ 없음 | 이승욱 |
| **관리자 웹** | ⛔ 0% (admin-web 없음) | 김지민 |

> 백엔드 API는 거의 다 됐는데 **그걸 보여줄 핵심 화면(QT·성경·노트)이 통째로 없음.** 앱 첫 화면(Today QT)부터 비어 있어 시연 직결 리스크.

---

## 5. 재분배 관점 요약

- **즉시(1~2일)**: bible 500 + bible_verses 시드(이지윤) — demo-blocking, 1줄~소량.
- **서버 잔여(완성도)**: AI 재시도·RUNNING 복구·사용자 Q&A(강상민), qt CRUD·audit 조회(강태오), sharing 토큰공유 정리/삭제 API(이승욱).
- **시연 직결(최대 투입)**: Flutter QT·성경·노트·달력 화면 — 이승욱(팀장) 주도로 이지윤(QT/성경)·김지민(노트/달력)에 집중 분배. S-02 API 연결·S-03 작성도.
- **관리자 웹**: 미착수 — 시연 범위 포함 여부 결정 필요.
- **저작자/문서 정합**: 김태혁 산출물·6/1 일정표가 미머지 브랜치에만 있음 → 머지 정리 필요. simulator 백엔드 실제 작성자(이지윤/강태오) 반영해 소유권 재정의.
