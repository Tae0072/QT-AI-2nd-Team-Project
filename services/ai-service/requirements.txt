# AI Service — Python FastAPI 의존성
# ⚠️ Spring Boot / Java 의존성 금지 — Python만

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Anthropic Claude API (SSE 스트리밍)
anthropic==0.39.0

# Vector Store (DECISIONS.md §6 기준)
chromadb==0.5.18

# Kafka Producer (ai.session.completed 발행)
kafka-python==2.0.2

# JWT 검증 (RS256)
python-jose[cryptography]==3.3.0

# SSE 서버 사이드 이벤트
sse-starlette==2.1.3

# HTTP 클라이언트 (내부 서비스 호출 시)
httpx==0.27.2

# Settings / Validation
pydantic==2.9.2
pydantic-settings==2.6.0

# Observability (Jaeger)
opentelemetry-distro==0.48b0
opentelemetry-exporter-otlp==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0

# Logging
structlog==24.4.0

# Dev tools
ruff==0.7.1
pytest==8.3.3
pytest-asyncio==0.24.0
